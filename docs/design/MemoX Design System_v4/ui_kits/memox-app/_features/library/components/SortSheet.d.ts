/**
 * Library sort-by bottom sheet.
 *
 * The kit renders this as a **static fixture** (hardcoded options + node ids) and
 * exposes **no props**. The Flutter `SortSheet` parameterizes the `current` sort
 * and wires `onSelect` — recorded as fixture-parameterized exceptions.
 */
export interface SortSheetProps {}

export function SortSheet(): JSX.Element;
