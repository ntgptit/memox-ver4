/**
 * Library overflow bottom sheet: Import / Export / Select multiple / Settings.
 *
 * The kit renders this as a **static fixture** (hardcoded items + node ids) and
 * exposes **no props**. The Flutter `OverflowMenuSheet` wires `onImport` /
 * `onExport` / `onSettings` — recorded as fixture-parameterized exceptions.
 */
export interface OverflowMenuSheetProps {}

export function OverflowMenuSheet(): JSX.Element;
