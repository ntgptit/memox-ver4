/**
 * Study-session stage 5 / answer-save-error: type the answer.
 *
 * The kit renders this as a **static fixture** (hardcoded prompt) and exposes **no
 * props**. The Flutter `StageTyping` parameterizes meaningLabel / meaning /
 * hintShown / hint, binds the field to a `controller`, and wires onHint / onCheck
 * — recorded as exceptions.
 */
export interface StageTypingProps {}

export function StageTyping(): JSX.Element;
