/**
 * Study-session stage 4: recall the meaning, then reveal it.
 *
 * The kit renders this as a **static fixture** (hardcoded term) and exposes **no
 * props**. The Flutter `StageRecall` parameterizes term / meaning / revealed and
 * wires onReveal / onNext — recorded as fixture-parameterized exceptions.
 */
export interface StageRecallProps {}

export function StageRecall(): JSX.Element;
