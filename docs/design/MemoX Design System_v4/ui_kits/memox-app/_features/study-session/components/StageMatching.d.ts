/**
 * Study-session stage 2: two columns of tiles (terms / meanings) to match.
 *
 * The kit renders this as a **static fixture** (hardcoded tile lists) and exposes
 * **no props**. The Flutter `StageMatching` parameterizes the state — terms /
 * meanings / matched / selectedTermId + onSelectTerm / onSelectMeaning — recorded
 * as fixture-parameterized exceptions.
 */
export interface StageMatchingProps {}

export function StageMatching(): JSX.Element;
