/**
 * Study-session stage 1: see the term + meaning, then advance.
 *
 * The kit renders this as a **static fixture** (hardcoded term/meaning) and
 * exposes **no props**. The Flutter `StageReview` parameterizes term / meaning and
 * wires onNext — recorded as fixture-parameterized exceptions.
 */
export interface StageReviewProps {}

export function StageReview(): JSX.Element;
