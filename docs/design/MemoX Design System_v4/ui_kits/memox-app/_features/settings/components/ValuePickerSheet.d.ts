/**
 * Settings words-per-round picker bottom sheet.
 *
 * The kit renders this as a **static fixture** (hardcoded 5/10/20 options +
 * selection) and exposes **no props**. The Flutter `ValuePickerSheet`
 * parameterizes `current` and wires `onSelect` — recorded as fixture-parameterized
 * exceptions.
 */
export interface ValuePickerSheetProps {}

export function ValuePickerSheet(): JSX.Element;
