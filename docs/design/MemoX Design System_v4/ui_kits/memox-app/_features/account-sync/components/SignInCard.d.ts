/**
 * Account-sync signed-out hero + Google sign-in.
 *
 * The kit renders this as a **static fixture** (hardcoded copy) and exposes **no
 * props**. The account-sync screen is DEFERRED (WBS S.22) — there is no Flutter
 * counterpart yet, recorded as a `deferred-screen` exception.
 */
export interface SignInCardProps {}

export function SignInCard(): JSX.Element;
