/**
 * Flashcard-editor duplicate-term warning callout with View existing / Add anyway.
 *
 * The kit renders this as a **static fixture** (hardcoded term + node ids) and
 * exposes **no props**. The Flutter `DupBanner` is likewise self-contained
 * (`const DupBanner()`) — copy from ARB — so both sides expose no props.
 */
export interface DupBannerProps {}

export function DupBanner(): JSX.Element;
