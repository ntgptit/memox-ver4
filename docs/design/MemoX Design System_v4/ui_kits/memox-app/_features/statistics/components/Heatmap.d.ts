/**
 * Statistics 14-week study-calendar grid.
 *
 * The kit renders this as a **static fixture** (hardcoded intensity pattern) and
 * exposes **no props**. The Flutter `Heatmap` parameterizes `days` — recorded as
 * a fixture-parameterized exception.
 */
export interface HeatmapProps {}

export function Heatmap(): JSX.Element;
