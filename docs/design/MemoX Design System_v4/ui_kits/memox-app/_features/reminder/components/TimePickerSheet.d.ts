/**
 * Reminder hour:minute picker bottom sheet.
 *
 * The kit renders this as a **static fixture** (hardcoded column values +
 * selection) and exposes **no props**. The Flutter `TimePickerSheet` is a
 * ConsumerWidget that reads the time from a provider and wires `onDone` —
 * recorded as a fixture-parameterized exception.
 */
export interface TimePickerSheetProps {}

export function TimePickerSheet(): JSX.Element;
