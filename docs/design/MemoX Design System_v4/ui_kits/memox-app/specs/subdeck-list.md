# subdeck-list — Nested Decks — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/subdeck-list/SubdeckList.jsx`; reference PNGs `../shots/subdeck-list--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs subdeck-list` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Browse and manage the nested decks inside the current deck.

Primary CTA: **Create deck**

## Archetype (contract §2)

List

## Composition map (contract §3)

Top → bottom: **nested MxContextualAppBar** → primary content → primary action (Create deck).
Exact layout order is owned by the source (`../_features/subdeck-list/SubdeckList.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxButton`, `MxCard`, `MxContextualAppBar`, `MxFab`, `MxIconButton`, `MxLink`, `MxList`, `MxScaffold`, `window.Breadcrumb`, `window.DeckPlaySheet`

## State matrix (contract §6) — 15 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `loaded` |
| 2 | `dense` |
| 3 | `deep` |
| 4 | `empty` |
| 5 | `search` |
| 6 | `no-results` |
| 7 | `selection` |
| 8 | `create-sheet` |
| 9 | `subdeck-actions` |
| 10 | `long-menu` |
| 11 | `play` |
| 12 | `loading` |
| 13 | `offline` |
| 14 | `error` |
| 15 | `not-found` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
