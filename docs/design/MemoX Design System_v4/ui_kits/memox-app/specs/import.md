# import — Import — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/import/Import.jsx`; reference PNGs `../shots/import--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs import` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Import cards from a file or pasted text into a deck.

Primary CTA: **Import**

## Archetype (contract §2)

Form

## Composition map (contract §3)

Top → bottom: **nested MxContextualAppBar** → primary content → primary action (Import).
Exact layout order is owned by the source (`../_features/import/Import.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxButton`, `MxCard`, `MxChip`, `MxContextualAppBar`, `MxIconButton`, `MxList`, `MxScaffold`, `window.ActionCallout`, `window.EmptyState`, `window.ListRow`, `window.ProgressBar`, `window.SectionLabel`

## State matrix (contract §6) — 7 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `source` |
| 2 | `mapping` |
| 3 | `preview` |
| 4 | `dup-warning` |
| 5 | `importing` |
| 6 | `import-error` |
| 7 | `done` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
