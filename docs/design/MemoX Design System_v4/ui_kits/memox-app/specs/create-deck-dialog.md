# create-deck-dialog — Create Deck · Dialog — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/create-deck-dialog/CreateDeckDialog.jsx`; reference PNGs `../shots/create-deck-dialog--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs create-deck-dialog` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Create another deck once a learning context exists: name + language pair in a modal dialog.

Primary CTA: **Create deck**

## Archetype (contract §2)

Form

## Composition map (contract §3)

Top → bottom: **ghost MxContextualAppBar** → primary content → primary action (Create deck).
Exact layout order is owned by the source (`../_features/create-deck-dialog/CreateDeckDialog.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxButton`, `MxCard`, `MxContextualAppBar`, `MxLink`, `MxScaffold`, `window.ActionCallout`, `window.FormDialog`

## State matrix (contract §6) — 12 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `root-default` |
| 2 | `root-multi-pair` |
| 3 | `root-missing-pair` |
| 4 | `nested` |
| 5 | `optional-expanded` |
| 6 | `validation` |
| 7 | `duplicate-root` |
| 8 | `duplicate-sibling` |
| 9 | `submitting` |
| 10 | `submit-failure` |
| 11 | `long-name` |
| 12 | `keyboard-open` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
