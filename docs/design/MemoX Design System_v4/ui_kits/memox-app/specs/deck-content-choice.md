# deck-content-choice — Deck Content Choice — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_shared/DeckContentChoice.jsx`; reference PNGs `../shots/deck-content-choice--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs deck-content-choice` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Decide how a new, empty deck is organised (subdecks vs cards).

Primary CTA: _none — a read / navigation surface (no competing CTA)_

## Archetype (contract §2)

Detail

## Composition map (contract §3)

Top → bottom: **nested MxContextualAppBar** → primary content → no primary action.
Exact layout order is owned by the source (`../_shared/DeckContentChoice.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxCard`, `MxContextualAppBar`, `MxIconTile`, `MxLink`, `MxScaffold`, `MxTextField`, `window.SectionLabel`

## State matrix (contract §6) — 1 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `default` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
