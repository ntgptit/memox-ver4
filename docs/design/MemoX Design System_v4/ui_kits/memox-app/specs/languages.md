# languages — Language Pairs — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/languages/Languages.jsx`; reference PNGs `../shots/languages--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs languages` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Manage learning↔native language pairs.

Primary CTA: **Add language pair**

## Archetype (contract §2)

List

## Composition map (contract §3)

Top → bottom: **nested MxContextualAppBar** → primary content → primary action (Add language pair).
Exact layout order is owned by the source (`../_features/languages/Languages.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxButton`, `MxCard`, `MxContextualAppBar`, `MxIconButton`, `MxScaffold`, `window.EmptyState`, `window.ListRow`, `window.SectionLabel`

## State matrix (contract §6) — 5 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `list` |
| 2 | `one` |
| 3 | `empty` |
| 4 | `add` |
| 5 | `remove` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
