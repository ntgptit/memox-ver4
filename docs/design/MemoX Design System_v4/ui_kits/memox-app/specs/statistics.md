# statistics — Stats — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/statistics/Statistics.jsx`; reference PNGs `../shots/statistics--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs statistics` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Review study progress, streaks and retention.

Primary CTA: _none — a read / navigation surface (no competing CTA)_

## Archetype (contract §2)

Dashboard

## Composition map (contract §3)

Top → bottom: **root MxContextualAppBar** → primary content → no primary action.
Exact layout order is owned by the source (`../_features/statistics/Statistics.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxBottomNav`, `MxButton`, `MxCard`, `MxContextualAppBar`, `MxScaffold`, `MxSectionHeader`, `MxSegmentedControl`, `window.EmptyState`, `window.Skeleton`, `window.Stat`

## State matrix (contract §6) — 5 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `loaded` |
| 2 | `scope-switch` |
| 3 | `insufficient` |
| 4 | `loading` |
| 5 | `error` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
