# fill-mode — Fill — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/fill-mode/FillMode.jsx`; reference PNGs `../shots/fill-mode--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs fill-mode` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Type the term from its meaning (session stage 5).

Primary CTA: **Check**

## Archetype (contract §2)

Focused task/study flow

## Composition map (contract §3)

Top → bottom: **nested MxContextualAppBar** → primary content → primary action (Check).
Exact layout order is owned by the source (`../_features/fill-mode/FillMode.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxButton`, `MxCard`, `MxContextualAppBar`, `MxIconButton`, `MxScaffold`, `window.EmptyState`, `window.Note`, `window.ProgressHeader`, `window.SectionLabel`

## State matrix (contract §6) — 6 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `waiting` |
| 2 | `typing` |
| 3 | `hint` |
| 4 | `correct` |
| 5 | `wrong` |
| 6 | `complete` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
