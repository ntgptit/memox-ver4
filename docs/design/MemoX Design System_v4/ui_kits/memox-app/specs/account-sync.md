# account-sync — Account & Sync — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/account-sync/AccountSync.jsx`; reference PNGs `../shots/account-sync--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs account-sync` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Sign in and manage cloud sync of decks and progress.

Primary CTA: **Sign in**

## Archetype (contract §2)

Detail

## Composition map (contract §3)

Top → bottom: **primary MxContextualAppBar** → primary content → primary action (Sign in).
Exact layout order is owned by the source (`../_features/account-sync/AccountSync.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxBadge`, `MxButton`, `MxCard`, `MxContextualAppBar`, `MxIconButton`, `MxScaffold`, `window.KeyboardInset`, `window.ProfileCard`

## State matrix (contract §6) — 5 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `signed-out` |
| 2 | `signed-in` |
| 3 | `syncing` |
| 4 | `conflict` |
| 5 | `offline` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
