# match-mode — Match — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/match-mode/MatchMode.jsx`; reference PNGs `../shots/match-mode--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs match-mode` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Match terms to meanings (session stage 2).

Primary CTA: **Next round**

## Archetype (contract §2)

Focused task/study flow

## Composition map (contract §3)

Top → bottom: **nested MxContextualAppBar** → primary content → primary action (Next round).
Exact layout order is owned by the source (`../_features/match-mode/MatchMode.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxButton`, `MxContextualAppBar`, `MxIconButton`, `MxScaffold`, `window.EmptyState`, `window.ProgressHeader`

## State matrix (contract §6) — 6 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `playing` |
| 2 | `selected` |
| 3 | `correct` |
| 4 | `wrong` |
| 5 | `almost` |
| 6 | `complete` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
