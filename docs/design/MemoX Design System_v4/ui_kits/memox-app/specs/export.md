# export — Export — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/export/Export.jsx`; reference PNGs `../shots/export--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs export` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Export a deck’s cards to a shareable file format.

Primary CTA: **Export**

## Archetype (contract §2)

Form

## Composition map (contract §3)

Top → bottom: **nested MxContextualAppBar** → primary content → primary action (Export).
Exact layout order is owned by the source (`../_features/export/Export.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxButton`, `MxCard`, `MxChip`, `MxContextualAppBar`, `MxIconButton`, `MxScaffold`, `MxSegmentedControl`, `MxSwitch`, `window.EmptyState`, `window.ListRow`, `window.SectionLabel`

## State matrix (contract §6) — 4 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `config` |
| 2 | `exporting` |
| 3 | `export-error` |
| 4 | `done` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
