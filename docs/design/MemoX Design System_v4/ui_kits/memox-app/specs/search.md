# search — Search — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/search/Search.jsx`; reference PNGs `../shots/search--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs search` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Find a card or deck by word or meaning.

Primary CTA: _none — a read / navigation surface (no competing CTA)_

## Archetype (contract §2)

Search

## Composition map (contract §3)

Top → bottom: **search MxContextualAppBar** → primary content → no primary action.
Exact layout order is owned by the source (`../_features/search/Search.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxCard`, `MxContextualAppBar`, `MxIconButton`, `MxLink`, `MxList`, `MxScaffold`, `window.EmptyState`, `window.ListRow`, `window.SectionLabel`, `window.Skeleton`

## State matrix (contract §6) — 5 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `empty-recent` |
| 2 | `results` |
| 3 | `filtered` |
| 4 | `no-results` |
| 5 | `loading` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
