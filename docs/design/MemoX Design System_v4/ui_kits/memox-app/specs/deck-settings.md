# deck-settings — Deck Settings — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/deck-settings/DeckSettings.jsx`; reference PNGs `../shots/deck-settings--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs deck-settings` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Manage a deck’s metadata and lifecycle actions.

Primary CTA: **Save**

## Archetype (contract §2)

Settings

## Composition map (contract §3)

Top → bottom: **nested MxContextualAppBar** → primary content → primary action (Save).
Exact layout order is owned by the source (`../_features/deck-settings/DeckSettings.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxButton`, `MxContextualAppBar`, `MxIconButton`, `MxScaffold`

## State matrix (contract §6) — 6 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `action-sheet` |
| 2 | `leaf-actions` |
| 3 | `rename` |
| 4 | `move` |
| 5 | `reset-confirm` |
| 6 | `delete-confirm` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
