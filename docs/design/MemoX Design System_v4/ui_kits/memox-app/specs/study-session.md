# study-session — Study Session (5 stages) — screen spec

> AUTOGENERATED from tool/ui_kit_shots/registry.mjs — do not hand-edit; run `npm run gen:ui-kit`.
> Source of truth: `../_features/study-session/StudySession.jsx`; reference PNGs `../shots/study-session--<state>--<theme>.png` (390×780).
> Verify: `node tool/ui_kit_shots/shoot.mjs study-session` (gated by `npm run verify:ui-kit`).

## Objective (contract §1)

Run the 5-stage study session end to end.

Primary CTA: **Next**

## Archetype (contract §2)

Focused task/study flow

## Composition map (contract §3)

Top → bottom: **ghost MxContextualAppBar** → primary content → primary action (Next).
Exact layout order is owned by the source (`../_features/study-session/StudySession.jsx`) and the reference PNGs — read those for pixel
composition; the component map below is the authoritative set of pieces in play.

## Component map (contract §3) — auto-derived from source

`MxButton`, `MxCard`, `MxContextualAppBar`, `MxIconButton`, `MxScaffold`, `window.Note`, `window.ProgressHeader`

## State matrix (contract §6) — 10 states, each rendered light + dark

| # | state |
| --- | --- |
| 1 | `stage1-review` |
| 2 | `stage2-match` |
| 3 | `stage3-guess` |
| 4 | `stage4-recall` |
| 5 | `stage5-fill` |
| 6 | `relearn` |
| 7 | `due-review` |
| 8 | `exit` |
| 9 | `resume-error` |
| 10 | `answer-save-error` |

## Handoff notes

Spacing scale `{4,8,12,16,24,32,48}`; screen padding 16; tokens only (no raw hex / off-scale).
Exactly one primary objective; touch targets ≥ 44×44. This spec is generated from
`tool/ui_kit_shots/registry.mjs` — edit the registry, not this file.
