# MemoX v4 — Production Implementation WBS

Turns the **UI Kit design system** (`docs/design/MemoX Design System_v4/`) into an implementation
plan for **Expo SDK 57 + React Native + Expo Router**, delivered as **vertical slices**:

```
Screen/state → domain/use case → repository → local persistence → tests → visual verification
```

## Source of truth

- **Screens / states / archetype / objective / CTA:** `tool/ui_kit_shots/registry.mjs` (26 screens · 168 states). This WBS does not rename or add/remove any screen or state.
- **Per-screen specs:** `docs/design/MemoX Design System_v4/ui_kits/memox-app/specs/<id>.md`.
- **Canonical reference PNGs:** `docs/design/MemoX Design System_v4/ui_kits/memox-app/shots/<id>--<state>--<theme>.png` (336).
- **Construction rules:** `docs/design/mobile-ui-construction-contract.md`.
- **Design system tokens/components:** `docs/design/MemoX Design System_v4/tokens/*.css`, `.../components.css`, `.../components/**/Mx*.jsx` (18 primitives), `.../readme.md`, `.../SKILL.md`.

## Repository reality (baseline `ce0f8bb`)

Production is a **fresh Expo Router scaffold**, not MemoX. Verified present: `src/app/{index,explore,_layout}.tsx`, `src/components/*` (starter widgets — `themed-text`, `app-tabs`, `animated-icon`, …), `src/constants/theme.ts`, `src/hooks/use-color-scheme.ts`, `src/global.css`, `scripts/reset-project.js`, `assets/`. **Absent:** `app/` at root, any `Mx*` production component, any domain / repository / persistence / feature code, any production test. Therefore **no** feature is `Implemented` or `Partial`; every business row is `Specified` or `Blocked`. The UI Kit being complete does **not** make any production feature `Implemented`.

## Status contract

| Status | Meaning |
|---|---|
| `Implemented` | Production source + tests + docs evidence exist |
| `Partial` | Real implementation exists but acceptance not fully met |
| `Specified` | Enough information for an agent to start |
| `Blocked` | Missing dependency, decision, or upstream work |
| `Future` | Outside current MVP |
| `Deprecated` | No longer used |

Only rows marked `Implemented`/`Partial` cite a real repo-root implementation path in **Evidence/Source**. `Specified`/`Blocked` rows cite the **spec** source (registry, specs, shots, contract) and name the **target** production path to be created (marked *(to create)*, never claimed as existing).

## Dependency identifiers

Every token in a **Dependencies** cell is either a **WBS id** (`N.N`) or a **capability id** from the Dependency Approval Register (`DEP-…`). Section-level wildcards, ad-hoc dependency labels and placeholder groups are not used — a dependency is always a concrete work-package id or a registered capability id.

## Row legend

- **Parallel** = may run concurrently with peers. `No` = touches a shared surface (Expo Router tree, design-system primitives, DB schema, `wbs.md`) and must be serialized against other writers of that surface.
- **Owner:** `Opus` = Claude Code Opus (write). `Codex` = read-only audit/regression/scope guard. `Design` = Claude Design (composition map, kit mapping, screenshot comparison). `Human` = approval only.
- **Commit:** `TBD` until the package lands; then the squash-merge hash.

---

## 0. Governance and Architecture

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 0.1 | Production folder architecture | Define `src/` layout: `src/app` routes, `src/features/<feature>/{domain,data,ui}`, `src/design-system`, `src/shared`, `src/db`; retire starter widgets not reused | all | — | Implemented | P0 | No (defines shared tree) | ADR committed; every later target path derives from it; no starter route claimed as MemoX; folder rules match a chosen contract | `docs/adr/0001-architecture.md`; `src/{design-system,features,shared,db}/README.md` (skeleton) | Opus + Human | b6893dc |
| 0.2 | Expo Router strategy | Route tree + groups: `(tabs)` root nav, study `session` group, modal routes for sheets, typed routes; map registry screens → routes | app-bar, all | 0.1 | Implemented | P0 | No (owns route tree) | Every non-shared registry screen has one route; typed-routes build passes; back/deep-link behavior specified per screen | `docs/adr/0002-routing.md`; `src/app/**` (26 route files + layouts); `src/design-system/dev/route-placeholder.tsx` | Opus | 770c84f |
| 0.3 | State-management decision | Choose local UI/server-state approach (e.g. React Context + hooks vs a store) using only installed deps or an approved one; no premature global store | all | 0.1 | Implemented | P0 | No | ADR with rationale + rejected options; no new dependency added without register approval | `docs/adr/0003-state.md` (repository-hooks over `useSyncExternalStore` + focused Contexts; no store lib; 0 new deps) | Opus + Human | 9d767a0 |
| 0.4 | Local database decision | Evaluate local DB (`expo-sqlite`) as source of truth; schema outline for decks/subdecks/cards/languages/sessions/attempts/SRS; **decision gate that decides DEP-DB** | all persistence | 0.1 | Implemented | P0 | No | Candidate evaluated + **DEP-DB approved** (register → Approved); `expo-sqlite ~57.0.0` added (config plugin in `app.json`); offline source-of-truth confirmed; schema outline + access-layer contract drafted; unblocks 0.5 | `docs/adr/0005-db-schema.md`; `package.json` (`expo-sqlite ~57.0.0`); `app.json` (plugin) | Human | 44d21ab |
| 0.5 | Migration strategy | Versioned schema migrations + forward-only upgrade path + migration test harness design | all persistence | 0.4 | Implemented | P1 | No (owns schema) | Migration runner contract; rollback/`PRAGMA user_version` strategy; test plan for vN→vN+1 | `src/db/migrations/{types,runner,index}.ts` + `src/db/client.ts` (getDb/tx) + runner test harness | Opus | 9a6d350 |
| 0.6 | Error/result model | Define `Result<T,E>`/typed error model + user-facing error mapping used by every use case | all | 0.1, 0.13, DEP-TEST | Implemented | P0 | Yes | Discriminated result type; recoverable-error contract wired to every screen's error state; unit tests for mapping (needs the test harness, 0.13) | `src/shared/{result.ts,errors.ts,index.ts}` + `src/shared/__tests__/{result,errors}.test.ts` | Opus | 63ec1c9 |
| 0.7 | Repository/use-case contracts | Interface contracts for repositories + use cases (inputs/outputs/errors) independent of DB engine | sections 3–10 | 0.3, 0.6 | Implemented | P0 | Yes | TS interfaces compile; each feature domain references them; no UI or DB import in the domain layer | `src/shared/contracts/{use-case.ts,repository.ts,index.ts}` + conformance test `src/shared/contracts/__tests__/contracts.test.ts` | Opus | c8016ed |
| 0.8 | Test framework decision | Evaluate the candidate test framework (`jest-expo` + RNTL, DEP-TEST) + coverage tiers (unit/repo/integration/a11y/visual). **Decision gate that decides DEP-TEST — it does not depend on DEP-TEST.** No package install or test config here | 11.1–11.9 | 0.1 | Implemented | P0 | No | Candidate framework evaluated; **DEP-TEST approved** (register → Approved); no package installed / no test config in this row; unblocks the harness setup 0.13 | Dependency Approval Register (DEP-TEST = Approved); harness install/config is 0.13 | Human | e67934a |
| 0.9 | Dependency approval register | Maintain the register (below) as the single gate; any package used by an unapproved row keeps that row `Blocked` | all | — | Implemented | P0 | No | Register present and current; no package self-approved; every `Blocked` dep-row traces to a `DEP-*` line | This document — register audited 2026-07-14: every `package.json` dependency traces to an Approved `DEP-*` line (`better-sqlite3` retro-registered as DEP-DB-TESTDRIVER); all `Blocked` dep-rows trace to Pending `DEP-*` lines; standing user pre-approval for the WBS loop recorded in the register notes | Opus + Human | 9f6b396 |
| 0.10 | UI Kit → React Native mapping | Rulebook: token CSS → TS theme, `Mx*` base class → RN component, `.card`/variant modifiers → props; one-way Token→Component→Screen preserved | all components + screens | 0.1 | Implemented | P0 | Yes | Mapping table for all 18 primitives + variants; screen layer imports only components; components import only tokens | `docs/adr/0004-uikit-rn-mapping.md` (18-primitive table + token/idiom mapping); `docs/design/MemoX Design System_v4/components/**/Mx*.jsx` | Design + Opus | 86e8dd3 |
| 0.11 | `data-mx-node` → `testID` mapping | Rule to carry each kit `data-mx-node` id onto the RN node as `testID` for test + visual anchoring | all | 0.10 | Implemented | P1 | Yes | Deterministic id→testID rule; every screen spec's nodes covered; used by a11y/visual tests | `docs/adr/0006-testid-mapping.md` | Opus | dd962fe |
| 0.12 | Raw visual-value guard | Lint/guard forbidding raw hex/px/typography outside the design system in `src/features` and `src/app` | all components + screens | 0.1, 0.10 | Implemented | P1 | No (adds tooling) | Guard fails on a raw `#hex`/literal px color/off-scale spacing above the token layer; wired into verification | `eslint.config.js` (scoped `no-restricted-syntax` hex/rgb/hsl + `no-restricted-imports` raw-token ban over `src/features`/`src/app`, tests exempt) → runs in `npm run lint`; `docs/design/mobile-ui-construction-contract.md` §4; fixed the one pre-existing violation (`src/app/+not-found.tsx` → token-driven) | Opus | e814875 |
| 0.13 | Production test harness setup | Install + configure the approved test framework; create the `npm test` script; sample unit test, sample RN component test, and a sample repository/integration test contract; wire the test command into verification/CI per repo convention | 11.1–11.9 | 0.8, DEP-TEST | Implemented | P0 | No (adds config) | DEP-TEST approved by Human; test config exists; `npm test` runs; sample unit test passes; sample RN component test passes; sample repository/integration contract in place; no package added beyond the approved dependency | `jest.config.js`, `jest.setup.js`, `.github/workflows/test.yml`, `package.json` (`test` script + DEP-TEST devDeps); samples in `src/design-system/__tests__/`, `src/shared/testing/__tests__/` | Opus | 9cab3bd |

---

## 1. React Native Design System Runtime

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1.1 | Tokens → TypeScript | Map `tokens/*.css` (colors light+dark, spacing, radius, typography, elevation, motion, size, stroke, opacity, icon-size) to a typed theme object; names frozen | all | 0.1, 0.10 | Implemented | P0 | No (shared tokens) | Every `--memox-*` token has a typed value for light+dark; no value drift vs CSS; spacing restricted to {4,8,12,16,24,32,48} | `src/design-system/tokens/*.ts` (colors/spacing/radius/typography/elevation/motion/size + index) | Opus | 181e8d9 |
| 1.2 | Theme provider + light/dark | Theme context reading system scheme + user override; exposes tokens to components; dark via provider not per-component override | theme | 1.1 | Implemented | P0 | No | Toggling theme reskins all components from tokens only; no hard-coded colors; matches canonical light/dark shots | `src/design-system/theme/{theme.ts,theme-context.tsx,index.ts}`, mounted in `src/app/_layout.tsx` | Opus | 5b5d1e3 |
| 1.3 | Plus Jakarta Sans fonts | Load variable font via `expo-font` (installed); weight tokens (400/600/700/800); tracking rules | all | 1.1 | Implemented | P0 | Yes | Font loads on Android/iOS/web; weight tokens resolve; no system-font fallback in rendered text | `assets/fonts/PlusJakartaSans.ttf`, `src/design-system/fonts/index.ts` (`useMemoxFonts`), splash-gated in `src/app/_layout.tsx`; web export bundles the asset | Opus | 0a0d1ef |
| 1.4 | Icon adapter | ONE canonical **Material Symbols Rounded** source across Android/iOS/web via `expo-font` (installed) bundling the Material Symbols Rounded variable-font asset (DEP-ICON-FONT — not yet in the repo). Do **not** use SF Symbols on iOS — that breaks parity with the kit. Adapter exposes the kit glyph set at icon-size tokens | all | 1.1, DEP-ICON-FONT | Implemented | P0 | No | DEP-ICON-FONT approved by Human; same Material Symbols Rounded glyph renders on Android, iOS and web; no SF Symbols on iOS; no emoji; weight, FILL and icon size come from design tokens; deterministic missing-glyph fallback behaviour; the font is bundled locally so runtime does not depend on a CDN. (Automated tests for this adapter, incl. the missing-glyph fallback, are owned by 1.8.) | `assets/fonts/MaterialSymbolsRounded.ttf` (subset); `src/design-system/icons/{Icon.tsx,glyphs.ts,index.ts}` (+ glyph-map test); registered in `src/design-system/fonts/` | Opus | 5732082 |
| 1.5 | Surface primitives | RN `MxScaffold`, `MxContextualAppBar`, `MxCard`, `MxSectionHeader`, `MxIconTile`, `MxList` with frozen names/variants + `testID` from node | app-bar, all | 0.10, 0.11, 1.1, 1.4 | Implemented | P0 | No (shared) | Variants match kit base classes; ≥44×44 targets; light/dark + 320–430px per shots; a11y roles + `testID` contract implemented. **MxList**: canonical wrapper for any stack of cards (decks/subdecks/cards/results); default item gap uses the 12px token `--memox-space-3` (optional `gap` override); list screens wrap card lists in MxList and never drop cards straight into the scroll body (whose 24px section gap is wrong for items). (MxList fixtures + accessibility + token-parity tests are owned by 1.8.) | `src/design-system/components/surfaces/*` (MxScaffold/MxContextualAppBar/MxCard/MxSectionHeader/MxIconTile/MxList) + render smoke test; full a11y/parity suite owned by 1.8 | Opus + Design | 5dcee46 |
| 1.6 | Navigation primitives | RN `MxBottomNav`, `MxFab`, `MxSearchDock`, `MxIconButton` | app-bar, dashboard, library | 0.10, 0.11, 1.1, 1.4 | Implemented | P0 | No (shared) | Active-item pill + FAB placement match shots; hit areas ≥48px; keyboard/focus states present | `src/design-system/components/navigation/*` (MxBottomNav/MxFab/MxSearchDock/MxIconButton) + render smoke test; full a11y/parity owned by 1.8 | Opus + Design | 5f5c34e |
| 1.7 | Core input/action primitives | RN `MxButton`, `MxLink`, `MxTextField`, `MxChip`, `MxBadge`, `MxSwitch`, `MxSegmentedControl`, `MxAvatar` | all | 0.10, 0.11, 1.1, 1.4 | Implemented | P0 | No (shared) | Variants/modifiers only (no new names); contrast pairs pass AA; disabled/focus/error states; matches shots | `src/design-system/components/core/*` (MxButton/MxLink/MxTextField/MxChip/MxBadge/MxSwitch/MxSegmentedControl/MxAvatar) + render smoke test; full a11y/parity owned by 1.8 | Opus + Design | cbafafe |
| 1.8 | Fixtures + accessibility + parity tests | Component fixtures per state; a11y (roles, labels, target size); token-parity + `testID` presence tests for all 18 primitives (incl. MxList); the icon-adapter missing-glyph fallback test | all | 1.4, 1.5, 1.6, 1.7, 0.13, DEP-TEST | Implemented | P1 | No | Fixtures + accessibility + token-parity tests exist for all 18 primitives; the 1.4 icon adapter's missing-glyph fallback is tested; MxList has its fixture + a11y + token-parity test (gap-token asserted); token parity asserts no raw values; needs the test harness (0.13, DEP-TEST) | `src/design-system/components/__tests__/{a11y,parity}.test.tsx` + per-family render smokes; icon fallback in `src/design-system/icons/__tests__/` | Opus + Codex | dd3a736 |

> The 18 production primitives = surface (6: MxScaffold, MxContextualAppBar, MxCard, MxSectionHeader, MxIconTile, MxList) + navigation (4: MxBottomNav, MxFab, MxSearchDock, MxIconButton) + core (8: MxButton, MxLink, MxTextField, MxChip, MxBadge, MxSwitch, MxSegmentedControl, MxAvatar).

---

## 2. App Shell, Navigation and Theme — maps `app-bar`, `theme`

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2.1 | App shell + router layout | Root `(tabs)` layout, `MxScaffold` + `MxBottomNav` + fixed app bar; safe-area + FAB slot | app-bar (root-top/scrolled/unread) | 0.1, 0.2, 1.5, 1.6 | Implemented | P0 | No (owns shell) | Tabs route between placeholders; app bar elevates on scroll; safe-area/notch handled; matches app-bar shots | `.../specs/app-bar.md`, `.../shots/app-bar--root-*.png` | Opus | 2506c42 |
| 2.2 | Contextual app-bar variants | Nested/overflow/search/selection/modal app-bar variants + title truncation behavior | app-bar (nested, nested-overflow, search, selection, modal) | 2.1, 1.5 | Implemented | P0 | No (shared shell) | All 8 app-bar states render; overflow truncates without clipping meaning; selection count + actions correct | `.../specs/app-bar.md`, `.../shots/app-bar--*.png` | Opus + Design | 1045b8f |
| 2.3 | Theme screen + persistence | `theme` screen: appearance (light/dark/system), accent picker, text-size; persist choice; live preview | theme (light, dark, accent-size) | 1.2, 1.7, 0.5 | Implemented | P1 | No | Choice persists across restart using the approved DB decision (0.4) and the migration/storage strategy (0.5); preview updates live; accent swatches labeled + ≥48px; light/dark/font-scale 1.5 verified | `.../specs/theme.md`, `.../shots/theme--*.png`; persistence needs 0.5 (hard prerequisite, gated by 0.4) | Opus | 4e567ba |
| 2.4 | Shell/theme state matrix + tests | State-matrix fixtures + interaction + a11y tests for shell + theme | app-bar, theme | 2.1, 2.2, 2.3, 0.13, 0.11, DEP-TEST | Implemented | P1 | Yes | Every app-bar/theme state tested; testID anchors present; framework required (DEP-TEST) | `src/design-system/components/__tests__/shell-theme-matrix.test.tsx` (+ appbar-states, theme-screen, app-screen tests); AppScreen `main` slot + scroll anchor | Opus + Codex | acf29fe |
| 2.5 | Shell/theme visual verification | Render shell + theme states, compare to canonical shots, file numbered defects | app-bar, theme | 2.4 | Implemented | P1 | Yes | Screenshot compare vs `shots/`; 0 unaccepted overflow/clip; defect report filed + resolved | `docs/testing/2.5-shell-theme-verification.md`; rendered via `expo start --web` (`.claude/launch.json` app-web) — verified by a11y tree + computed styles | Blocked: waiting for shell/theme render states and the test framework through 2.4; `.../shots/{app-bar,theme}--*.png` | Design | 05c5caa |
| 2.6 | Feature quality harness readiness | Readiness gate confirming the shared test, accessibility and visual-regression harnesses are ready before any production feature screen begins | all | 11.1, 11.3, 11.4 | Implemented | P0 | No (readiness gate) | Test harness baseline (11.1) complete; accessibility tooling baseline (11.3) complete; visual regression harness (11.4) complete; the shared fixture, accessibility helper and visual-diff contract are usable by the feature screen slices; **does not claim any production feature screen is complete** — it is only a readiness gate for the vertical feature screen slices | `docs/testing/feature-screen-readiness.md` (gate PASSED: 11.1/11.3/11.4 confirmed ready + per-slice contract); Vertical Slice Quality Ownership (below) | Opus + Codex + Design | 2505754 |

---

## 3. Content Foundation — maps `languages`, `library`, `subdeck-list`, `deck-content-choice`

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 3.1 | Deck/subdeck/language domain | Entities + use cases (create/rename/move/delete deck & subdeck; add/remove language pair; deck-vs-cards organisation) using 0.7 contracts | languages, library, subdeck-list, deck-content-choice | 0.6, 0.7, 0.13, DEP-TEST | Implemented | P0 | Yes | Pure-TS domain compiles; use cases return typed results; no UI/DB imports; unit tests cover invariants (no orphan subdeck) | `src/features/library/domain/*` (deck/subdeck/ports/use-cases), `src/features/languages/domain/*`, `src/shared/contracts/services.ts` + domain tests | Opus | dad0b98 |
| 3.2 | Content persistence + repositories | Tables + repositories for decks/subdecks/cards-count/languages; transactional multi-table writes | languages, library, subdeck-list | 0.4, 0.5, 3.1, 0.13, DEP-TEST | Implemented | P0 | No (owns schema) | Restart preserves data; multi-table create/move is transactional with rollback test; DB is source of truth | migration `src/db/migrations/001-content.ts`; `src/db/sql.ts` (SqlDatabase port); `src/features/library/data/*` (repos+mappers); real-SQLite test harness `src/shared/testing/sqlite-test-db.ts` | Opus | 90302db |
| 3.3 | Languages slice | `languages` screen + list/one/empty/add/remove states + interactions wired to 3.1/3.2 | languages (list, one, empty, add, remove) | 3.1, 3.2, 1.5, 1.6, 1.7, 2.6 | Implemented | P1 | No | All 5 states; add/remove persists; empty + recoverable-error states; light/dark/320–430px/font-scale 1.5 | `src/features/languages/ui/*` (LanguagesScreen presentational + `use-languages` controller + `LanguagesContainer` + fixtures), route `src/app/settings/languages.tsx`, `src/shared/runtime.ts` (id/clock impls); tests `.../ui/__tests__/{languages-screen,a11y,use-languages}.test.tsx` (39); golden `tool/app_golden/baseline/languages--{list,empty,add}--light.png` + `--list--dark.png` | Opus + Design | ff0f6ef |
| 3.4 | Library slice | `library` screen: deck grid/list, search, filter, selection, create sheet, offline/loading; wired to 3.1/3.2 | library (12 states) | 3.1, 3.2, 4.6, 1.5, 1.6, 1.7, 2.6 | Implemented | P0 | No | All 12 states; search/filter/selection functional; FAB clearance; loading/empty/offline/error; visual parity | `src/features/library/ui/{library-screen,library-container,use-library,library-fixtures}.tsx/ts` + tests (state matrix, interactions, a11y); shared `DeckCard`/`Skeleton` composites in `src/design-system/components/composites/`; route `src/app/(tabs)/library.tsx`; goldens `tool/app_golden/baseline/library--*.png` — all 12 states < 3% vs kit (parity gate) | Opus + Design | 917d017 |
| 3.5 | Subdeck-list slice | `subdeck-list` screen: browse/manage subdecks, deep nesting, actions sheet, play, states | subdeck-list (13 states) | 3.1, 3.2, 1.5, 1.6, 1.7, 2.6 | Implemented | P0 | No | All 13 states incl. deep/error/offline; subdeck actions persist; breadcrumb nav; visual parity | `src/features/library/ui/{subdeck-list-screen,subdeck-list-container,use-subdeck-list,subdeck-list-fixtures,subdeck-card}.tsx/ts` + tests; shared `Breadcrumb`/`DeckPlaySheet` composites; route `src/app/deck/[deckId]/index.tsx`; goldens `subdeck-list--*.png` — all 13 states < 3% vs kit (parity gate) | Opus + Design | 6fcdfd3 |
| 3.6 | Deck-content-choice slice | `deck-content-choice`: choose subdecks-vs-cards organisation for a new empty deck | deck-content-choice (default) | 3.1, 3.2, 1.5, 1.7, 2.6 | Implemented | P1 | No | Choice sets deck organisation + persists; routes to subdeck-list or flashcard-list; single default state matches shot | `src/features/library/ui/*` (DeckContentChoiceScreen + `use-deck-content-choice` controller + container + fixtures), domain `reorganiseDeck`/`setDeckContentUseCase` in `src/features/library/domain/{deck,use-cases}.ts`, route `src/app/deck/[deckId]/content.tsx` (routes subdecks→`/deck/[id]`, cards→`/deck/[id]/cards`); tests `.../ui/__tests__/{deck-content-choice-screen,deck-content-choice-a11y,use-deck-content-choice}` + `.../domain/__tests__/deck-content` (26); golden `deck-content-choice--{default,named}--light.png` + `--default--dark.png` | Opus | 0577b88 |

---

## 4. Flashcard Authoring and Search — maps `flashcard-list`, `flashcard-editor`, `deck-settings`, `search`

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 4.1 | Flashcard domain | Card entity + use cases (create/edit/duplicate-detect/delete/additional-translation) + validation rules | flashcard-list, flashcard-editor | 0.6, 0.7, 3.1, 0.13, DEP-TEST | Implemented | P0 | Yes | Domain compiles; duplicate-detection + validation unit-tested; term/meaning/tags/audio-ref modeled | `src/features/flashcards/domain/*` (card/card-translation/duplicate/ports/use-cases) + domain tests | Opus | f9f2627 |
| 4.2 | Flashcard persistence + repository | Cards table + repository; deck card-count maintenance; transactional edits | flashcard-list, flashcard-editor | 0.4, 0.5, 4.1, 3.2, 0.13, DEP-TEST | Implemented | P0 | No (schema) | Restart preserves cards; edit/delete transactional; count stays consistent (rollback test) | `src/db/migrations/002-flashcards.ts` (card/card_translation + deck.card_count triggers); `src/features/flashcards/data/*` (card + translation repos, mappers) — real-SQLite tested | Opus | 8c7cea6 |
| 4.3 | Flashcard-list slice | `flashcard-list` screen: browse/filter/select/add-sheet/card-actions/delete-confirm + dense/min/long-text/offline/error | flashcard-list (15 states) | 4.1, 4.2, 1.5, 1.6, 1.7, 2.6 | Implemented | P0 | No | All 15 states; long/minimum data render without clipping; delete-confirm; visual parity + font-scale 1.5 | `src/features/flashcards/ui/{flashcard-list-screen,flashcard-list-container,use-flashcard-list,flashcard-list-fixtures}.tsx/ts` + tests; shared `StatusCardRow` composite; route `src/app/deck/[deckId]/cards.tsx`; goldens `flashcard-list--*.png` — all 15 states + dark < 3% vs kit (parity gate) | Opus + Design | c0571c3 |
| 4.4 | Flashcard-editor slice | `flashcard-editor` form: create/edit/validation/duplicate/additional-translation + submit lifecycle (submitting/error/success) + audio-generating | flashcard-editor (9 states) | 4.1, 4.2, 1.5, 1.7, DEP-TTS, 2.6 | Implemented | P0 | No | Full submit lifecycle (validation/disabled/submitting/failure/success); audio-generating gated on DEP-TTS; primary Save reachable over keyboard | `src/features/flashcards/ui/{flashcard-editor-screen,flashcard-editor-container,use-flashcard-editor,flashcard-editor-fixtures,flashcard-editor-components}.tsx/ts` + tests (create/edit via domain use cases; duplicate→conflict with View-existing/Add-anyway force save; deck-driven language labels from the deck's language pair; pronunciation via `expo-speech` — DEP-TTS Approved; sticky SaveBar in the new AppScreen `footer` slot under `KeyboardAvoidingView`); shared `Spinner` composite; icon font re-subset (+folder/sell/check/check_box/check_box_outline_blank/expand_less); routes `src/app/card/{new,[cardId]}.tsx`; goldens `flashcard-editor--*.png` — all 9 states + dark < 3% vs kit (parity gate) | Opus + Design | 977ea90 |
| 4.5 | Deck-settings slice | `deck-settings`: metadata + lifecycle (rename/move/reset-confirm/delete-confirm) via action sheet | deck-settings (5 states) | 3.1, 3.2, 1.5, 1.7, 2.6 | Implemented | P1 | No | All 5 states; destructive actions use danger + confirm; move/reset/delete persist with rollback test | `src/features/library/ui/*` (DeckSettingsScreen — Modal-based action-sheet/rename-dialog/move-sheet/reset+delete danger confirms — + `use-deck-settings` controller + container + fixtures), domain `moveDeck`/`moveDeckUseCase`, transactional `resetDeckProgress` in `src/features/session/data/reset-deck-progress.ts` (+ rollback test), route `src/app/deck/[deckId]/settings.tsx`; tests `.../library/{ui/__tests__/deck-settings-*,domain/__tests__/deck-content(move)}` + `.../session/data/__tests__/reset-deck-progress` (46); golden `deck-settings--{action-sheet,rename,move,reset-confirm,delete-confirm}--light` + `action-sheet--dark` | Opus | c253e3f |
| 4.6 | Search slice + index | `search` screen + local query over cards/decks (word/meaning); recent/results/filtered/no-results/loading | search (5 states), library-search | 4.2, 3.2, 1.5, 1.7, 2.6 | Implemented | P1 | No | Query returns from DB; recent history persists; no-results quotes query; debounced; visual parity | `src/features/search/data/*` (`searchLibrary` — LIKE over card term/meaning + deck join + SRS status, escaped wildcards; `recent-searches` over app_setting), `src/features/search/ui/*` (SearchScreen — dock + filter chips + result rows + recent + skeleton/no-results — + debounced `use-search` controller + container + fixtures), route `src/app/search.tsx`; tests `.../search/{data/__tests__/{search-query,recent-searches},ui/__tests__/{search-screen,search-a11y,use-search}}` (39); golden `search--{empty-recent,results,filtered,no-results}--light` + `results--dark` | Opus + Design | 9e2aa3c |

---

## 5. Study Entry, Dashboard and Session Core — maps `dashboard`, `mode-picker`, `study-session`

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 5.1 | Study/session domain + SRS | Session lifecycle (start/resume/finalize), attempt model, 5-stage ordering, SRS scheduling use cases | dashboard, mode-picker, study-session | 0.6, 0.7, 4.1, 0.13, DEP-TEST | Implemented | P0 | Yes | Session state machine + SRS scheduling unit-tested; due/relearn logic deterministic; resume rebuilds state | `src/features/session/domain/*` (stage/srs/session/ports/use-cases) + domain tests | Opus | f7b220c |
| 5.2 | Session + progress persistence | Sessions/attempts/SRS tables + repository; durable mid-session state for resume | study-session, dashboard, statistics | 0.4, 0.5, 5.1, 0.13, DEP-TEST | Implemented | P0 | No (schema) | Kill/restart mid-session resumes losslessly; attempts persist per stage; SRS updates transactional (rollback test) | `src/db/migrations/003-sessions.ts` (srs_state/session/attempt); `src/features/session/data/*` (repos, mappers, transactional persistAnswer) — real-SQLite tested (resume + rollback) | Opus | 66c2154 |
| 5.3 | Dashboard slice | `dashboard` screen: due summary, streak/goal, continue-CTA, onboarding/empty/loading | dashboard (8 states) | 5.1, 5.2, 1.5, 1.6, 1.7, 2.6 | Implemented | P0 | No | All 8 states incl. caught-up/streak-reset/goal-met/empty/loading; numbers from DB; FAB clearance; visual parity | `src/features/dashboard/ui/{dashboard-screen,dashboard-container,use-dashboard,dashboard-fixtures,dashboard-components}.tsx/ts` + tests (streak/goal/mastery derived from decks+SRS+sessions); shared `ProgressBar`/`Note` composites, `DeckCard.progress`, kit `contrast` button + root-bar bell/avatar; route `src/app/(tabs)/index.tsx`; goldens `dashboard--*.png` — all 8 states + dark < 3% vs kit (parity gate) | Opus + Design | 1b28f85 |
| 5.4 | Mode-picker slice | `mode-picker`: choose mode + scope, not-enough-words guard, scope dropdown | mode-picker (default, scope-dropdown, not-enough) | 5.1, 5.2, 1.5, 1.7, 2.6 | Implemented | P1 | No | All 3 states; not-enough explains threshold + offers fix; scope selection feeds session start | `src/features/session/ui/*` (ModePickerScreen + scope bottom-sheet + `use-mode-picker` controller + container + model/fixtures), route `src/app/session/mode-picker.tsx` (routes to `/session/<mode>?deckId=&scope=`); tests `.../ui/__tests__/{mode-picker-screen,mode-picker-a11y,use-mode-picker}` (25); golden `mode-picker--{default,scope-dropdown,not-enough}--light` + `--default--dark` | Opus | 108951d |
| 5.5 | Study-session shell slice | `study-session` orchestrator: 5-stage flow shell, progress header, exit/resume-error/answer-save-error, due-review/relearn | study-session (10 states) | 5.1, 5.2, 6.1, 6.2, 6.3, 7.1, 7.2, 2.6 | Implemented | P0 | No | All 10 states; exit=danger; resume-error + answer-save-error recover; progress bar + n/N; stages mount from 6.1/6.2/6.3/7.1/7.2 | `src/features/session/ui/{study-session-screen,study-session-container,use-study-session,study-session-fixtures,study-session-components}.tsx/ts` + tests (full 5-stage orchestration: start/RESUME a `full` session with position rebuilt from attempts — broken resume → resume-error; per-answer `recordAnswer` with the answer-save-error retry dialog on failure; wrong guesses re-queue as not-counted relearn passes; due-review mode over `srs.dueCards`; finalize at the last stage; session-wide n/N = distinct (stage,card) over cards×5); route `src/app/session/play.tsx`; goldens `study-session--*.png` — all 10 states + dark < 3% vs kit (parity gate) | Opus + Design | 6a25d19 |

---

## 6. Study Modes 1–3 — maps `review-mode`, `match-mode`, `guess-mode`

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 6.1 | Review-mode slice | `review-mode` (stage 1): browse round, inline edit, audio, end; persists attempts | review-mode (browsing, editing, audio, loading, error, end) | 5.1, 5.2, 1.5, 1.7, DEP-TTS, 2.6 | Implemented | P0 | No | All 6 states; edit persists; audio gated on DEP-TTS; loading/error/end; progress header; visual parity | `src/features/session/ui/{review-mode-screen,review-mode-container,use-review-mode,review-mode-fixtures}.tsx/ts` + tests (browse round starts a `review` session; ONE attempt per card via `recordAnswer` grade `good` on pass, never re-recorded on prev; inline meaning edit persists via `editCardUseCase`; pronunciation via `expo-speech`; finalize at round end); shared `ProgressHeader`/`StudyPromptCard` reused; route `src/app/session/review.tsx`; goldens `review-mode--*.png` — all 6 states + dark < 3% vs kit (parity gate) | Opus + Design | 69fa2e1 |
| 6.2 | Match-mode slice | `match-mode` (stage 2): term↔meaning grid, selected/correct/wrong/almost/complete | match-mode (playing, selected, correct, wrong, almost, complete) | 5.1, 5.2, 1.5, 1.7, 2.6 | Implemented | P0 | No | All 6 states; wrong pairs use color+non-color cue; count label; attempts persisted; visual parity | `src/features/session/ui/*` (MatchModeScreen — two-column tile board; correct pair locks green ✓, wrong flashes red ✕ (colour **and** icon), matched tiles dim + announce disabled — + `use-match-mode` session-play controller + pure `match-board` builder + container + fixtures), route `src/app/session/match.tsx`; controller reuses 7.1 session-play (start→correct pair persists via `recordAnswer` `good`→finalize when the board is fully matched); tests `.../ui/__tests__/{match-board,match-mode-screen,match-mode-a11y,use-match-mode}` (28); golden `match-mode--{playing,selected,correct,almost}--light` + `playing--dark` | Opus + Design | 230db0a |
| 6.3 | Guess-mode slice | `guess-mode` (stage 3): pick meaning, correct/wrong/long-text/complete | guess-mode (waiting, correct, wrong, long-text, complete) | 5.1, 5.2, 1.5, 1.7, 2.6 | Implemented | P0 | No | All 5 states; feedback = color+icon; long-text no clip; attempts persisted; visual parity | `src/features/session/ui/*` (GuessModeScreen — prompt + 5 options, feedback marks correct ✓ green + wrong ✕ red (colour **and** icon), long text wraps — + `use-guess-mode` session-play controller + pure `guess-options` builder + container + fixtures + shared `study-chrome`), route `src/app/session/guess.tsx`; controller reuses 7.1 session-play (start→pick persists via `recordAnswer` correct→`good`/wrong→`again`→continue→finalize); tests `.../ui/__tests__/{guess-options,guess-mode-screen,guess-mode-a11y,use-guess-mode}` (29) + shared `src/shared/testing/session-fakes.ts`; golden `guess-mode--{waiting,correct,wrong,long-text}--light` + `waiting--dark` | Opus + Design | fcbaeb0 |

---

## 7. Study Modes 4–5, Player and Finalization — maps `recall-mode`, `fill-mode`, `player`, `study-result`

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 7.1 | Recall-mode slice | `recall-mode` (stage 4): reveal + self-grade forgot/remembered | recall-mode (before-reveal, revealed, forgot, remembered, complete) | 5.1, 5.2, 1.5, 1.7, 2.6 | Implemented | P0 | No | All 5 states; grade pair equal-width ≥48px; grade persists to SRS; visual parity | `src/features/session/ui/*` (RecallModeScreen — progress header + prompt card + reveal/meaning panel + equal-width Forgot/Got-it grades + round-complete — + `use-recall-mode` **session-play** controller + container + fixtures), route `src/app/session/recall.tsx`; the controller starts a session (5.1), persists each grade via `recordAnswer` (5.2 transactional attempt+SRS; forgot→`again` re-queues, remembered→`good`), finalizes on completion; tests `.../ui/__tests__/{recall-mode-screen,recall-mode-a11y,use-recall-mode}` (23); golden `recall-mode--{before-reveal,revealed,remembered,complete}--light` + `revealed--dark` | Opus + Design | 1705f42 |
| 7.2 | Fill-mode slice | `fill-mode` (stage 5): type term, hint, correct/wrong; keyboard-aware | fill-mode (waiting, typing, hint, correct, wrong, complete) | 5.1, 5.2, 1.5, 1.7, 2.6 | Implemented | P0 | No | All 6 states; check reachable over keyboard; hint + char-compare; attempts persisted; visual parity | `src/features/session/ui/*` (FillModeScreen — meaning prompt + `TextInput` + Help/Check, wrapped in `KeyboardAvoidingView`; hint note; per-character `CharCompare` diff + answer on wrong; accept/retry — + `use-fill-mode` session-play controller + container + fixtures), route `src/app/session/fill.tsx`; controller reuses 7.1 session-play (start→check `normalize`d answer persists via `recordAnswer` correct→`good`/wrong→`again`; accept→`good` typo override; retry re-answers; finalize on completion); tests `.../ui/__tests__/{fill-mode-screen,fill-mode-a11y,use-fill-mode}` (26); golden `fill-mode--{waiting,hint,correct,wrong}--light` + `wrong--dark` | Opus + Design | ca4c6f4 |
| 7.3 | Player slice | `player`: hands-free audio playback of a deck, transport, speed, error/end | player (playing, paused, speed, error, end) | 5.1, 5.2, 1.5, 1.7, DEP-TTS, 2.6 | Implemented | P1 | No | All 5 states; transport ≥48px; audio gated on DEP-TTS; error/end handled; visual parity | `src/features/session/ui/{player-screen,player-container,use-player,player-fixtures}.tsx/ts` + tests (hands-free loop: each card speaks term→meaning via `expo-speech` at the chosen rate and auto-advances on done; pause stops speech, play resumes; ×0.75/×1/×1.5 segments re-drive; speech failure → error+retry; end panel replays); icon font +`pause`; route `src/app/player.tsx`; goldens `player--*.png` — all 5 states + dark < 3% vs kit (parity gate) | Opus + Design | 300337c |
| 7.4 | Study-result + finalization slice | `study-result`: summary, goal-met/missed, many-wrong, finalizing/retry/finalize-error; commit SRS + streak | study-result (7 states) | 5.1, 5.2, 7.1, 7.2, 2.6 | Implemented | P0 | No | All 7 states; finalize is transactional with retry on error; result numbers from DB; visual parity | `src/features/session/ui/{study-result-screen,study-result-container,use-study-result,study-result-fixtures,study-result-components}.tsx/ts` + tests (finalize via `finalizeSessionUseCase` with retry; cards/correct/minutes/wrong/streak/goal derived from session+attempt history); shared `Stat` composite; route `src/app/session/result.tsx`; goldens `study-result--*.png` — all 7 states + dark < 3% vs kit (parity gate) | Opus + Design | 5b48622 |

---

## 8. Progress and Reminder — maps `statistics`, `reminder`

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 8.1 | Statistics slice | `statistics`: streaks/retention/heatmap/bars, scope switch, insufficient/loading/error | statistics (loaded, scope-switch, insufficient, loading, error) | 5.2, 7.4, 2.6 | Implemented | P1 | No | All 5 states; charts have axis labels; numbers from DB; scope re-derives; visual parity | `src/features/stats/ui/{statistics-screen,statistics-container,use-statistics,statistics-fixtures,statistics-components}.tsx/ts` + tests (streaks current/longest, 14-week heatmap intensity, minutes-per-weekday bars, Leitner reps→box 1–8 histogram, 30-day accuracy Donut, library overview — all derived from decks+sessions+attempts+SRS; `insufficient` under 10 attempts; This-pair/All scope re-derives); shared `Ring` composite over `react-native-svg` (DEP-SVG Approved); route `src/app/(tabs)/stats.tsx`; goldens `statistics--*.png` — all 5 states + dark < 3% vs kit (parity gate) | Opus + Design | 1f5d837 |
| 8.2 | Reminder slice | `reminder`: daily study reminder on/off + time-picker; schedule local notifications | reminder (on, off, time-picker) | 0.5, 1.5, 1.7, DEP-NOTIFICATIONS, 2.6 | Implemented | P1 | No | All 3 states; scheduling gated on DEP-NOTIFICATIONS; choice persists across restart via the migration/storage strategy (0.5); day-chips + time render per shots | `src/features/settings/ui/{reminder-screen,reminder-container,use-reminder,reminder-fixtures}.tsx/ts` + `src/features/settings/data/reminder-settings.ts` (typed config over the app_setting repo — restart-safe, corrupt JSON → default) + tests (every change persists AND reschedules; hour:minute picker emits HH:MM; off dims + disarms the time/day controls); scheduling via `expo-notifications` weekly triggers per chosen weekday (DEP-NOTIFICATIONS Approved; permissions requested lazily on enable; web no-op); route `src/app/settings/reminders.tsx`; goldens `reminder--*.png` — all 3 states + dark < 3% vs kit (parity gate) | Opus | c41c9ac |

---

## 9. Import and Export — maps `import`, `export`

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 9.1 | Import slice | `import`: source/mapping/preview/dup-warning/importing/import-error/done; parse file or pasted text → cards | import (7 states) | 4.1, 4.2, 1.5, 1.7, DEP-FILE-PICKER, 2.6 | Implemented | P1 | No | All 7 states; column mapping + dup handling; transactional bulk insert with rollback; file picker gated on DEP-FILE-PICKER | `src/features/flashcards/ui/{import-screen,import-container,use-import,import-components,import-fixtures}.tsx/ts` + `src/features/flashcards/domain/import.ts` (`parseImportRows` tab/comma/semicolon with header skip + 1-based row-numbered failures; `countDuplicateRows` over normalized terms; `importCards` validates ALL rows before any write, per-card progress, and a mid-batch save failure rolls back the already-saved cards — compensated all-or-nothing) + shared `ActionCallout` composite (kit `_shared`); file picking via `expo-document-picker` (DEP-FILE-PICKER Approved; web `File.text()`, native uri fetch); tests (parser/dup/rollback, controller source→mapping→preview/dup-warning→importing→done/import-error + retry + column swap, screen 7-state matrix + interactions, a11y labels/progressbar/AA); route `src/app/settings/import.tsx`; goldens `import--*.png` — all 7 states + dark < 3% vs kit (parity gate, max 1.77%) | Opus + Design | 8c3ebd5 |
| 9.2 | Export slice | `export`: config/exporting/error/done; write deck to shareable file format | export (config, exporting, export-error, done) | 4.2, 1.5, 1.7, DEP-FILE-SHARING, 2.6 | Implemented | P1 | No | All 4 states; export format valid + re-importable; error/done states; sharing gated on DEP-FILE-SHARING | `src/features/flashcards/ui/{export-screen,export-container,use-export,export-components,export-fixtures}.tsx/ts` + `src/features/flashcards/domain/export.ts` (`serializeExport` — header + term/meaning per line, optional Leitner Box/Due columns, term-side separator/newline sanitizing; `exportDeck` — scope 'deck' root-only vs 'subtree', per-card SRS join + progress) with a TESTED import round-trip (serialize → 9.1 `parseImportRows` returns the cards verbatim); delivery via `expo-file-system` cache write + `expo-sharing` share sheet (DEP-FILE-SHARING Approved; web = blob download/clipboard; copy format = clipboard/share-sheet, no file); icon subset +`radio_button_checked` (125 glyphs); `ListRow` gained `selected` a11y state, `EmptyState` gained `blockAction` (kit block button in the action slot); tests (serializer/round-trip/scope/SRS-join, controller config→exporting→done/export-error + retry + share/save uri, screen 4-state matrix + interactions, a11y radio selection/progressbar/AA); route `src/app/settings/export.tsx`; goldens `export--*.png` — all 4 states + dark < 3% vs kit (parity gate, max 1.88%) | Opus + Design | c1cc147 |

---

## 10. Settings, Account, Sync and Backup — maps `settings`, `account-sync`

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 10.1 | Settings hub slice | `settings`: navigation hub + study sub-screens (word-display/SRS/mode/voice) + value-picker | settings (loaded, study-hub, study-worddisplay, study-srs, study-mode, study-voice, value-picker) | 2.3, 5.2, 1.5, 1.7, DEP-TTS, 2.6 | Implemented | P1 | No | All 7 states; sub-settings persist + feed study behavior; value-picker; voice gated on DEP-TTS; visual parity | `src/features/settings/ui/{settings-screen,settings-container,use-settings,settings-fixtures}.tsx/ts` + `src/features/settings/data/study-settings.ts` (typed StudySettings over the app_setting repo — restart-safe, shape-validated, corrupt JSON → kit defaults) + shared `ProfileCard`/`SelectSheet` composites (kit `_shared`); root = the Profile tab (`src/app/(tabs)/profile.tsx` — profile card · STUDY entry · APP rows routing to theme/reminders/export; Backup awaits 10.3), hub + children on `src/app/settings/study.tsx?screen=`; words-per-round ValuePickerSheet persists via the value-picker; **feeds study behavior**: the 5.5 study-session container decorates its card repo with a Fisher-Yates order when `shuffle` is on; voice screen persists tts/stt (DEP-TTS Approved); icon subset +`circle` (126 glyphs); tests (persistence defaults/corrupt/round-trip, controller load/toggle-persists-whole-config/picker-commit, screen 7-state matrix + interactions, a11y switch labels/selection/AA); goldens `settings--*.png` — all 7 states + dark < 3% vs kit (parity gate, max 2.31%) | Opus + Design | d4fc849 |
| 10.2 | Account & sync slice | `account-sync`: sign-in + cloud sync states signed-out/in/syncing/conflict/offline | account-sync (5 states) | 5.5, 7.4, DEP-AUTH, DEP-CLOUD, 2.6 | Blocked | P2 | No | All 5 states; the 5-stage study session works (5.5) and session finalization + SRS update work (7.4) before this starts — account/sync must not begin before a usable learning flow; sign-in gated on DEP-AUTH; conflict resolution defined | `.../specs/account-sync.md`, `.../shots/account-sync--*.png` | Opus + Human | TBD |
| 10.3 | Backup & restore | Local backup export + restore import; cloud backup gated on approved provider | account-sync, settings | 4.2, 5.2, 5.5, 7.4, DEP-FILE-PICKER, DEP-FILE-SHARING, DEP-CLOUD, 2.6 | Blocked | P2 | No | Local backup export; local restore import; restore validation before write; backup/restore round-trips losslessly; must not begin before a usable learning flow (5.5 + 7.4); cloud backup only runs when DEP-CLOUD is approved | `.../specs/account-sync.md`; Dependency Approval Register | Opus + Human | TBD |

---

## 11. Quality and Release

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 11.1 | Test harness baseline and quality contract | Test pyramid; folder convention; shared fixtures; sample unit/component/repository tests; coverage + command contract; the rule that **every vertical slice owns its own tests** | all | 0.13, DEP-TEST | Implemented | P0 | Yes | Test harness works; a shared fixture convention exists; sample tests present at each supported tier; the contract requires every feature slice to add its tests in that slice's own PR; **this row does not claim that all feature tests are complete** | `docs/testing/quality-contract.md`; `src/shared/testing/fixtures.ts` (+ test); `src/shared/testing/sqlite-test-db.ts`; Vertical Slice Quality Ownership (below) | Opus + Codex | 900a269 |
| 11.2 | Repository + migration tests | Transactional multi-table write + rollback + vN→vN+1 migration tests | 3.2, 4.2, 5.2 | 0.4, 0.5, 0.13, DEP-TEST | Implemented | P0 | No (schema) | Rollback on failure proven; migrations forward-only + tested; DB source-of-truth asserted | `src/db/__tests__/migrations-integration.test.ts` (real v0→v3 chain, idempotent re-run, multi-table rollback, source-of-truth); repo rollback tests in 3.2/4.2/5.2; runner tests in 0.5 | Opus | d275f2c |
| 11.3 | Accessibility tooling baseline | Role/label convention; target-size rule; font-scale rule; contrast-check approach; shared accessibility test helpers | all | 1.8, 2.4, 0.13, DEP-TEST | Implemented | P1 | Yes | Shared accessibility checks work on the design-system + shell fixtures; a mandatory per-slice contract exists; **does not claim an audit of all production screens while those screens do not yet exist** | `docs/testing/accessibility-contract.md`; `src/shared/testing/a11y.ts` (contrast + helpers) + `src/shared/testing/__tests__/a11y.test.ts` (token AA matrix); Vertical Slice Quality Ownership (below) | Codex + Design | 0ce7f75 |
| 11.4 | Visual regression harness | Choose + configure the golden/screenshot approach; canonical-shot lookup; naming convention; diff-artifact convention; baseline sample for the shell or one fixture | all | 0.13, 2.5, DEP-GOLDEN, DEP-TEST | Implemented | P1 | Yes | Golden solution approved (DEP-GOLDEN); harness runs on the baseline sample; diff artifacts are produced and reviewable; each feature slice adds its own canonical screen states; **does not claim golden coverage of all RN screens while those features do not yet exist** | `tool/app_golden/` (shoot.mjs harness + README) + committed baseline `baseline/shell-dashboard--loaded--light.png`; `npm run golden`; DEP-GOLDEN approved | Design + Opus | b7eeb9b |
| 11.5 | Performance | List virtualization, session-flow responsiveness, cold-start, DB query budgets | library, subdeck-list, flashcard-list, study-session | 3.4, 3.5, 4.3, 5.5 | Implemented | P2 | Yes | Large decks scroll at target FPS; no N+1 DB reads; cold-start budget met | **DB query budgets / no N+1**: the library load previously issued 1 + 4 reads PER DECK (subdecks, card count, card ids, due states); now a fixed 4 reads total — new batch reads `CardRepository.countByDecks` (one `deck.card_count` IN-query over the trigger-maintained counter) and `SrsStateRepository.dueCountByDeck` (one `srs_state ⋈ card` GROUP BY) + `subdecks.list()`, wired through rewritten `useLibrary` deps; guarded by `use-library.test.ts` (50-deck library → exactly 4 dep calls) and SQLite tests for both batch reads. **List virtualization**: `AppScreen` gained a virtualized `body` mode (FlatList scroll container, header slot, 12px separators, initialNumToRender 12 / windowSize 7) and the UNBOUNDED list — flashcard-list browse/search/selection — runs on it (large decks mount lazily; guarded by the dense-state test asserting row 16 is NOT initially mounted); library/subdeck lists stay eager (bounded item counts, now O(1)-query). **Session-flow responsiveness**: per-answer writes remain the single transactional `persistAnswer` (5.2) — no per-render DB work. **Cold-start budget**: web export entry bundle recorded at 1.94 MB (snapshot for regression comparison). All 16 flashcard-list golden pairs unchanged ≤ 2.99% (parity gate green after the FlatList conversion) | Opus + Codex | d1f9bf4 |
| 11.6 | Android/iOS smoke | Device smoke of the content, learning and portability flows on both platforms | all | 3.3, 3.4, 3.6, 4.3, 4.4, 4.6, 5.5, 7.4, 9.1, 9.2, 10.3 | Blocked | P1 | No | On both Android and iOS: create language pair (3.3); create deck (3.4); choose deck content structure (3.6); create and edit flashcard (4.3, 4.4); browse and search content (3.4, 4.6); start, resume and finish a study session (5.5); finalize session and update SRS (7.4); import (9.1); export (9.2); backup and restore (10.3); safe area; keyboard; hardware/system back behaviour. No flow is called smoked unless its prerequisite slice is done | Device runs | Opus + Human | TBD |
| 11.7 | Backup compatibility | Forward/backward backup-format compatibility across schema versions | 10.3 | 10.3, 0.5 | Blocked | P2 | No | Old backup restores on new schema; format versioned | 10.3 | Opus | TBD |
| 11.8 | Release readiness | App icon/splash, store metadata, permissions review, build profiles | all | 4.6, 7.4, 10.3, 11.6 | Blocked | P2 | No | Signed builds; permissions justified; checklist complete | `app.json` | Opus + Human | TBD |
| 11.9 | Docs-code parity | Keep this WBS, ADRs and register in sync with merged commits | all | — | Implemented | P1 | Yes | Every merged slice updates the Traceability Log + row status/commit; 0 stale `Implemented` claims | Enforced as a standing per-slice step since the WBS loop began (every `feat(N.N)` lands with a paired `docs(11.9)` commit recording the hash in the row + Traceability Log) and closed with a full audit (2026-07-15): all 60 Implemented rows now cite their real landing commit (45 historical `TBD` cells backfilled from `feat/docs/test/chore(N.N)` history, every hash verified against git), the 6.2 Traceability Log `TBD` placeholder resolved to `230db0a`, status counts recomputed from the table (67 rows = 61 Implemented / 6 Blocked), evidence paths spot-verified on disk; remaining `TBD` commits appear only on the 6 Blocked rows, which is the documented convention | Opus + Codex | 1458db1 |

---

## 12. Navigation & Interaction Test Suite

Source: the 2026-07-15 navigation audit (`docs/reports/navigation-audit-2026-07-15.md`, tooling `tool/nav_audit/`). The parity gate proves screens LOOK right; this section proves the app WIRES right. Standing rules for every 12.x test row: (a) buttons deep in child screens are reached through the SAME path a user would take, over seeded data — deep links are allowed only as bug-workaround assertions until the blocking fix lands; (b) every back control (app-bar back, cancel, browser/hardware back) is in scope and must return to the exact parent state; (c) every dialog/sheet is asserted to OPEN and to CLOSE via each of its close affordances (action button, Cancel, scrim); (d) destructive confirms are verified against the DB (confirm mutates, cancel does not).

| ID | Work package | Scope | UI-kit mapping | Dependencies | Status | Priority | Parallel | Acceptance criteria | Evidence/Source | Owner | Commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 12.1 | Fix: create-deck flow | Wire the domain `createDeck` use case into `/deck/new/content` (audit P0-1: every Create-deck button dead-ends on deck id `new`) | deck-content-choice | 3.1, 3.6 | Implemented | P0 | No | From a fresh install, Create deck (dashboard, library empty-state, library create-sheet) produces a real named deck via `createDeck` and lands on its content screen; with NO language pair yet, the flow routes to add-pair instead of erroring; the nav-audit `BUG?` step flips to a positive assertion | `deck-content-choice-container.tsx` — the `/deck/new/content` sentinel branches to the domain `createDeck` (first language pair; `onNeedLanguagePair` routes to `/settings/languages` when none) and reports the CREATED id to `onChosen`; container test (creates with real id + no-pair routes to add-pair, nothing persisted); nav suite: fresh DB → Create deck redirects to add-pair → pair added → Create deck lands on `/deck/<id>/cards` and the library lists it (46/46 run) | Opus | 3e5aa53 |
| 12.2 | Fix: study-hub child navigation | Hub rows push `/settings/study?screen=…` — a same-pathname query-only push expo-router web swallows (audit P0-2) | settings | 10.1 | Implemented | P0 | Yes | Word display / SRS / Mode / Voice each open from a real hub-row press (own child pathnames or a params-object push); back returns to the hub; existing deep links keep working; the four `BUG?` steps flip | Children moved to their own pathnames `src/app/settings/study/{index,[section]}.tsx` — the param is `[section]` because `screen` is a RESERVED React Navigation param name (it was consumed before reaching the route, which was WHY the query push looked swallowed); hub rows push `/settings/study/<key>`; legacy `?screen=` deep links still render via the hub route; nav suite: all four hub rows open their child + back to hub + legacy deep link (46/46 run) | Opus | 3e5aa53 |
| 12.3 | Fix: session stage-2 match input | `StudySessionScreen` feeds tiles `onPickTile`, container only passes `onPickOption` — stage 2 unanswerable (audit P0-3) | study-session | 5.5 | Implemented | P0 | No | Match tiles answer stage 2 (correct grades `good`, wrong grades `again` + relearn tail); a full 5-stage session is completable end-to-end to the result screen; the `BUG?` step flips | `study-session-container.tsx` passes `onPickTile={ctrl.pickOption}` (the current card's meaning tile grades `good`, others `again` + relearn); nav suite completes a FULL live 5-stage session — review Next ×3, match tiles ×3, guess options ×3, recall Show ×3, fill type+check ×3 — through to `/session/result`, plus exit-dialog Stay/Leave (46/46 run) | Opus | 3e5aa53 |
| 12.4 | Fix: card-editor deck context | `/card/new` is pushed WITHOUT `deckId` from every entry point, editor falls back to a fixture deck name and Save fails validation after the form is filled (audit P1-4) | flashcard-editor | 4.4, 12.1 | Implemented | P1 | Yes | Every Add-card entry passes its real deck context (`deckId` + `subdeckId` from the cards screen); entry points with no deck in context resolve one explicitly (picker or default) or hide the action; Save from each entry persists into the correct deck (asserted in DB) | `deck/[deckId]/cards.tsx` pushes `/card/new?deckId=…(&subdeckId=…)`, mode-picker forwards its deck; `flashcard-editor-container.tsx` resolves the library's first deck when no context (import/export default); PLUS the audit-found overlay defect: sheets now CLOSE before navigating (flashcard-list add/actions, library create, subdeck create, deck-settings export) — an open sheet blocked the pushed screen on web and lingered on native; nav suite: FAB → Add card URL carries deckId, editor Save persists into THAT deck and the row appears (46/46 run) | Opus | 3e5aa53 |
| 12.5 | Nav-test harness + data seeding | Promote `tool/nav_audit` to a first-class suite (`npm run test:nav`): deterministic DB seeding so deep screens are reachable through real user paths | all | 0.5, 4.2, 5.2, 2.6 | Specified | P0 | No | One command exports/serves the web build and runs the Playwright suite headless with a CI exit code; a deterministic seed (language pairs, decks incl. a subdeck tree, cards with new/due/mastered SRS states, session history) is injected before the run; per-step PASS/FAIL report retained; `BUG?` assertions documented as flip-on-fix | `tool/nav_audit/{static,run}.mjs` (baseline 53/53) | Opus | TBD |
| 12.6 | Nav tests: shell & tabs | Tab bar, root app bars, stack back behaviour | app shell | 12.5 | Specified | P1 | Yes | All 4 tabs switch in both directions with state preserved per tab; browser/hardware back from every pushed screen returns to its exact parent (asserted per stack); +not-found renders and recovers; root-bar controls asserted (search opens `/search`; bell asserted once 12.11 decides it) | 11.4 harness + 12.5 suite | Opus | TBD |
| 12.7 | Nav tests: content tree | Library → deck → subdeck → flashcard-list drill-down + every content overlay | library, subdeck-list, deck-settings | 12.1, 12.5 | Specified | P1 | No | Drill-down over the seeded tree with back at EVERY level returning to the parent state; library create/filter sheets, subdeck create/actions sheets and all four deck-settings overlays (rename, move, reset, delete) each open AND close via button, Cancel and scrim; delete/reset confirm mutates the DB, cancel does not | 12.5 suite | Opus | TBD |
| 12.8 | Nav tests: card flows | Editor entry/exit from every source + card actions + search chain | flashcard-editor, flashcard-list, search | 12.4, 12.5 | Specified | P1 | No | Editor reached from deck cards, subdeck cards, dashboard, library and mode-picker with the right deck context; Save, keep-adding and Cancel each return to the correct screen; card-actions paths (edit → editor → back; delete → dialog → confirm removes the row, cancel keeps it); search → result → editor → back chain | 12.5 suite | Opus | TBD |
| 12.9 | Nav tests: study flows | Mode-picker, all 5 modes, full 5-stage session, exit dialog, player, result | mode-picker, study-session, all modes, player, study-result | 12.3, 12.5 | Specified | P0 | No | Scope sheet opens, picks and dismisses; each of the 5 modes entered from mode-picker over seeded due cards and exited via back mid-round; ONE full 5-stage session completed through every stage to the result screen and back to the deck; exit dialog asserted for both Stay and Leave mid-session; player transport (play, pause, next, prev, speed) plus back; result finalize-retry path | 12.5 suite | Opus | TBD |
| 12.10 | Nav tests: settings & tools | Settings tree, pickers, import/export journeys, reminders, theme | settings, import, export, reminder, theme | 12.2, 12.5 | Specified | P1 | No | Every settings root row navigates and backs; hub children via real presses; words-per-round picker and reminder time-picker open, commit and dismiss (scrim + Done) with the committed value visible after reload; theme change survives reload; import journey over the seeded deck incl. the dup-warning branch (seed duplicate terms) through done → Back to deck; export config → done with Share/Save present; back asserted mid-flow in both journeys | 12.5 suite | Opus | TBD |
| 12.11 | Dead controls: wire or remove | Move card, Hide card, subdeck Rename/Move/Delete, Import subdecks, dashboard bell (audit P1-5..7) | flashcard-list, subdeck-list, dashboard | Product decision | Blocked | P2 | Yes | Each control is either wired to a real flow (new WBS rows if screens are needed) or removed/hidden with a documented kit divergence; until decided the audit records them as known no-ops and 12.6–12.8 assert the documented behaviour | Audit defects 5–7; human product decision on wire-vs-remove | Opus + Human | TBD |

---

## Vertical Slice Quality Ownership

Quality is **not** deferred to Wave 11. Each screen slice (sections 3–10) owns its own quality work and adds it in the slice's own PR:

- **Interaction tests** — the slice's user actions behave per spec.
- **State-matrix tests** — every registry state for the screen renders (loading, empty, error, submit lifecycle, edge data).
- **Accessibility checks** — roles/labels, ≥44×44 targets, font-scale 1.5, AA contrast, using the shared helpers from 11.3.
- **Visual verification** — the slice's canonical screen states compared to `shots/` using the harness from 11.4.

Rows **11.1** (test harness baseline + quality contract), **11.3** (accessibility tooling baseline) and **11.4** (visual regression harness) provide **only** the shared harness and contract — they do not implement or complete any feature's tests, and they do not claim app-wide coverage.

The ordering is enforced by the **dependency graph**, not by an incidental DB block:

- The shell/theme rows **2.1–2.5** build the bootstrap fixtures the harnesses need; they therefore **must not** depend on 2.6 (that would be a cycle).
- The shared harnesses **11.1, 11.3, 11.4** consume those bootstrap fixtures and provide the shared fixture, accessibility-helper and visual-diff contracts.
- The readiness gate **2.6** depends on 11.1, 11.3 and 11.4 and confirms the harnesses are usable.
- **Every** production screen slice in sections 3–10 (the feature screen slices 3.3–10.3) depends on **2.6** — so no feature slice can start until the harnesses are ready. This is a real graph edge, not merely a side effect of the DB gate.

Each feature slice still **owns** its own interaction tests, state-matrix tests, accessibility checks and visual verification (using the shared harnesses); 2.6 and 11.1/11.3/11.4 only make those tools available.

---

## UI Kit Coverage

`app-bar` is shared design-system/shell work (rows 1.5–1.7, 2.1, 2.2), not a business feature screen.

| Screen | Registry states | WBS IDs | Missing states | Blocked reason |
|---|---:|---|---|---|
| dashboard | 8 | 5.1, 5.2, 5.3 | 0 | DEP-DB via 0.4/5.2; Quality readiness gate: 2.6 |
| library | 12 | 3.1, 3.2, 3.4 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| subdeck-list | 13 | 3.1, 3.2, 3.5 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| flashcard-list | 15 | 4.1, 4.2, 4.3 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| deck-settings | 5 | 3.1, 3.2, 4.5 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| deck-content-choice | 1 | 3.1, 3.2, 3.6 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| flashcard-editor | 9 | 4.1, 4.2, 4.4 | 0 | DEP-DB via 0.4; audio needs DEP-TTS; Quality readiness gate: 2.6 |
| mode-picker | 3 | 5.1, 5.2, 5.4 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| review-mode | 6 | 5.1, 5.2, 6.1 | 0 | DEP-DB via 0.4; audio needs DEP-TTS; Quality readiness gate: 2.6 |
| match-mode | 6 | 5.1, 5.2, 6.2 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| guess-mode | 5 | 5.1, 5.2, 6.3 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| recall-mode | 5 | 5.1, 5.2, 7.1 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| fill-mode | 6 | 5.1, 5.2, 7.2 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| player | 5 | 5.1, 5.2, 7.3 | 0 | DEP-DB via 0.4; audio needs DEP-TTS; Quality readiness gate: 2.6 |
| study-result | 7 | 5.1, 5.2, 7.4 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| search | 5 | 4.2, 4.6 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| statistics | 5 | 5.2, 8.1 | 0 | DEP-DB via 5.2; after finalization (7.4); Quality readiness gate: 2.6 |
| reminder | 3 | 8.2 | 0 | DEP-NOTIFICATIONS + DEP-DB; Quality readiness gate: 2.6 |
| account-sync | 5 | 10.2, 10.3 | 0 | DEP-AUTH + DEP-CLOUD; after learning flow (7.4); Quality readiness gate: 2.6 |
| theme | 3 | 1.2, 2.3 | 0 | DEP-DB via 0.4 |
| import | 7 | 4.1, 4.2, 9.1 | 0 | DEP-FILE-PICKER + DEP-DB; Quality readiness gate: 2.6 |
| export | 4 | 4.2, 9.2 | 0 | DEP-FILE-SHARING + DEP-DB; Quality readiness gate: 2.6 |
| languages | 5 | 3.1, 3.2, 3.3 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| study-session | 10 | 5.1, 5.2, 5.5 | 0 | DEP-DB via 0.4; Quality readiness gate: 2.6 |
| settings | 7 | 2.3, 5.2, 10.1 | 0 | DEP-DB via 0.4; voice needs DEP-TTS; Quality readiness gate: 2.6 |
| app-bar | 8 | 1.5, 1.6, 1.7, 2.1, 2.2 | 0 | Shared shell (not a feature screen) |

```
Unmapped production screens:  0
Unmapped registry states:     0   (168/168 covered)
```

---

## Dependency Approval Register

No package is self-approved. Any work package that depends on a `Pending` capability stays `Blocked`.

| ID | Capability | Candidate dependency | Needed by WBS | Status | Human decision required |
|---|---|---|---|---|---|
| DEP-DB | Local database | `expo-sqlite` `~57.0.0` | 0.4, 0.5, 3.2, 4.2, 5.2, 11.2 | **Approved** (2026-07-13, ADR 0005) | Yes — approved: offline source-of-truth confirmed |
| DEP-ICON-FONT | Canonical cross-platform icon font | Material Symbols Rounded variable-font asset (Apache-2.0; subset to the kit glyph set, bundled at `assets/fonts/MaterialSymbolsRounded.ttf`) | 1.4 | **Approved** (2026-07-13) | Yes — approved: Apache-2.0 license, sourced from google/material-design-icons, subset via pyftsubset to the 119 kit icons (~246 KB), bundled locally (no CDN); regenerate by re-subsetting from kit icon usage |
| DEP-FILE-PICKER | File picker (import) | `expo-document-picker` `^57.0.0` | 9.1, 10.3 | **Approved** (2026-07-14, standing WBS-loop pre-approval — named Expo-SDK package, flipped by row 9.1 on start; see Notes) | Yes — approved |
| DEP-FILE-SHARING | File sharing (export) | `expo-sharing` `~14.0.0` + `expo-file-system` `~19.0.0` | 9.2, 10.3 | **Approved** (2026-07-15, standing WBS-loop pre-approval — named Expo-SDK packages, flipped by row 9.2 on start; see Notes) | Yes — approved |
| DEP-NOTIFICATIONS | Local notifications | `expo-notifications` `^0.32.17` | 8.2 | **Approved** (2026-07-14, standing WBS-loop pre-approval — named Expo-SDK package, flipped by row 8.2 on start; see Notes). Permissions: requested lazily on first enable, weekly local triggers only, no push/remote | Yes — approved |
| DEP-TTS | Text-to-speech | `expo-speech` `~14.0.0` | 4.4, 6.1, 7.3, 10.1 | **Approved** (2026-07-14, standing WBS-loop pre-approval — named Expo-SDK package, flipped by row 4.4 on start; see Notes) | Yes — approved |
| DEP-AUTH | Google authentication | `expo-auth-session` / provider SDK | 10.2 | Pending | Yes — provider + OAuth config |
| DEP-CLOUD | Cloud/Drive backup | Provider SDK (TBD) | 10.2, 10.3, 11.7 | Pending | Yes — choose provider |
| DEP-TEST | Production test framework | `jest-expo` + `@testing-library/react-native` | 0.6, 0.8, 0.13, 1.8, 2.4, 3.1, 3.2, 4.1, 4.2, 5.1, 5.2, 11.1, 11.2, 11.3, 11.4 | **Approved** (2026-07-13) | Yes — approved; install/config in 0.13 |
| DEP-GOLDEN | Screenshot/golden tests | Playwright (chromium, already a dep) screenshots of the Expo **web** build, pixel-diffed with `pixelmatch`/`pngjs` against committed baselines | 11.4 | **Approved** (2026-07-13) | Yes — approved: Playwright-on-web-build golden harness (`tool/app_golden/`), deterministic + CI-friendly |
| DEP-DB-TESTDRIVER | Node SQLite driver for jest (test-only) | `better-sqlite3` + `@types/better-sqlite3` (devDeps) — backs the DEP-DB repository/migration tests (11.2) under jest, where `expo-sqlite` cannot run | 0.5, 3.2, 4.2, 5.2, 11.2 | **Approved** (2026-07-14, retro-registered by the 0.9 audit) | Yes — approved: test-only, never imported by production code |
| DEP-SVG | Vector charts (stats ring/arcs) | `react-native-svg` `^15.15.5` | 8.1 | **Approved** (2026-07-14, standing WBS-loop pre-approval — named Expo-SDK package, flipped by row 8.1 on start; see Notes) | Yes — approved |

Notes (2026-07-14, 0.9 audit): the project owner granted **standing pre-approval** for the autonomous WBS loop — Expo-SDK candidate capabilities with a named package (DEP-FILE-PICKER, DEP-FILE-SHARING, DEP-NOTIFICATIONS, DEP-TTS) may be flipped to Approved by the implementing row when it starts, citing this note; DEP-AUTH and DEP-CLOUD still require a human **provider choice** before approval. `expo-font`, `expo-router`, `expo-web-browser`, `react-native-*` are already in `package.json` (SDK 57) and need **no** new approval — section 1's tokens/theme/fonts and section 2's shell build on them. Row 1.4 loads its glyphs with the installed `expo-font`; the **Material Symbols Rounded font asset** (DEP-ICON-FONT, now **Approved**) is subset to the kit glyph set and bundled at `assets/fonts/MaterialSymbolsRounded.ttf` (no CDN). SF Symbols are deliberately not used (they would break cross-platform glyph parity with the kit).

---

## Agent Allocation

| Owner | Responsibilities |
|---|---|
| **Claude Code Opus** | Architecture, vertical-slice implementation, domain/data/persistence, multi-file production screens, tests, docs-code parity |
| **Codex** | Read-only audit, edge-case analysis, regression review, diff review, scope/dependency guard |
| **Claude Design** | Composition map, UI-kit component mapping, screenshot comparison vs `shots/`, visual defect reports, design-ambiguity review |
| **Human** | Dependency approval, database/schema direction, cloud provider, product behavior without a contract, priority/scope changes |

Concurrency rule: no two write agents (Opus instances) may modify a **shared surface** at once — the Expo Router tree (0.2, 2.1, 2.2), design-system primitives (1.5, 1.6, 1.7), the DB schema (0.4, 0.5, 3.2, 4.2, 5.2), or this `wbs.md`. Those rows are `Parallel = No` and serialized. Feature domain rows (3.1, 4.1, 5.1) and independent slices are `Parallel = Yes`.

---

## Critical Path

```
Governance and architecture (0.1–0.13)
→ RN design system (1.1–1.8)
→ app shell and bootstrap fixtures (2.1–2.5)
→ shared quality harnesses (11.1, 11.3, 11.4)
→ feature quality readiness gate (2.6)
→ content creation (3.1–3.6)
→ content management (4.1–4.6)
→ study entry/resume (5.1–5.5)
→ five-stage study flow (6.1–6.3, 7.1–7.3)
→ session finalization and SRS update (7.4)
→ statistics and progress (8.1–8.2)
→ import/export (9.1–9.2)
→ settings (10.1)
→ account/sync/backup (10.2, 10.3)
```

Statistics (8.1) follows session finalization (7.4) and does **not** block MVP B. Cloud/account work (10.2, 10.3) must **not** precede a usable learning flow (MVP B).

### MVP A — Content usable
Create language pair · create deck/subdeck · create + manage flashcards (incl. the audio-generating state) · search/browse · restart preserves data.
Covers: 0.1–0.13, 1.1–1.8, 2.1–2.6, the shared quality harnesses 11.1/11.3/11.4, 3.1–3.6, 4.1–4.6. Gates (all required): **DEP-DB, DEP-TEST, DEP-ICON-FONT, DEP-GOLDEN, DEP-TTS** approved (DEP-TTS because 4.4 includes the flashcard `audio-generating` state).

### MVP B — Learning usable
Start + resume session · complete five study stages · persist attempts · finalize session · update SRS · show result.
Covers: 5.1–5.5, 6.1–6.3, 7.1–7.4. Depends on MVP A + 0.4/0.5. (Statistics 8.1 is progress, delivered after 7.4 — it does not gate MVP B.)

### MVP C — Data portability
Import · export · backup · restore.
Covers: 9.1–9.2, 10.3, 11.7. Depends on MVP A/B + DEP-FILE-PICKER / DEP-FILE-SHARING / DEP-CLOUD approved.

---

## Next 10 Tasks

Ten packages deliverable from `main` now — governance and RN design-system foundation first, no feature screen jumped. Every task is either `Specified` with its dependencies completed by an earlier task here, or an explicit **Human decision gate**. Rows that became `Blocked` by a test/icon prerequisite (0.6, 0.7, 1.5–1.7) are intentionally **not** listed as deliverable now; `0.13` is excluded because it needs 0.8 + DEP-TEST approved first.

| # | WBS ID | Agent | Dependencies | Expected touched areas | Acceptance | Stop condition |
|---|---|---|---|---|---|---|
| 1 | 0.1 | Opus + Human | — | `docs/adr/0001-architecture.md` *(to create)*, folder plan for `src/` | ADR committed; target tree defined; starter widgets triaged | Stop if architecture needs a product decision not in the contract |
| 2 | 0.2 | Opus | 0.1 (task 1) | `src/app/_layout.tsx`, route-tree plan | Every non-shared registry screen mapped to one typed route | Stop if a screen has no clear route archetype |
| 3 | 0.3 | Opus + Human | 0.1 (task 1) | `docs/adr/0002-state.md` *(to create)* | State ADR with rationale + rejected options; no new dep without register approval | Stop if the state approach needs an unapproved dependency |
| 4 | 0.10 | Design + Opus | 0.1 (task 1) | Mapping rulebook doc | All 18 primitives + variants mapped Token→Component→Screen | Stop on any kit ambiguity → design review |
| 5 | 0.4 | Human | 0.1 (task 1) | Dependency register (DEP-DB), schema outline | **Human decision gate — no implementation before approval.** DEP-DB approved/rejected; schema outline ratified | No code/schema work until DEP-DB is approved |
| 6 | 0.8 | Human | 0.1 (task 1) | Dependency register (DEP-TEST) | **Human decision gate — no install/config before approval.** DEP-TEST approved/rejected (unblocks 0.13) | No package/test-config work until DEP-TEST is approved |
| 7 | 0.11 | Opus | 0.10 (task 4) | testID mapping rule doc | Deterministic `data-mx-node`→`testID` rule covering every spec node | Stop if a spec node has no stable id |
| 8 | 1.1 | Opus | 0.1 (task 1), 0.10 (task 4) | `src/design-system/tokens/` *(to create)* | All tokens typed for light+dark; no value drift vs CSS | Stop if a token has no CSS source |
| 9 | 1.2 | Opus | 1.1 (task 8) | Theme provider, `src/hooks/use-theme.ts` | Theme reskins from tokens only; matches light/dark shots | Stop if dark requires a non-token override |
| 10 | 1.3 | Opus | 1.1 (task 8) | `expo-font` load, typography tokens | Plus Jakarta Sans loads on all platforms; weights resolve | Stop if the variable font fails on a platform |

---

## Commit Traceability Log

Newest first. Update on every merged slice with the actual squash-merge hash and the WBS IDs it delivered.

| Commit | Date | WBS IDs | Summary |
|---|---|---|---|
| 230db0a | 2026-07-13 | 6.2 | WBS 6.2 Match-mode slice (session stage 2 — completes the non-TTS study modes): `src/features/session/ui/` — `MatchModeScreen` (prop-driven two-column tile board: meanings left, terms right; a correct pair locks **green ✓**, a wrong pair flashes **red ✕** — colour **and** icon; matched tiles dim + announce `disabled`) + `use-match-mode` session-play controller (tap a meaning + a term → match by `cardId`; correct locks + persists via `recordAnswer` `good`, wrong clears the selection; finalizes when the board is fully matched) + pure deterministic `match-board` builder (meanings in order, terms reversed so pairs never align, capped at `BOARD_SIZE`) + container. Reuses the 7.1 session-play pattern + shared `study-chrome`/`session-fakes`. Route `src/app/session/match.tsx` (live container; DB-free `?state=`/`?theme=` preview). Tests (28): pure board builder, screen state matrix + interactions (tap L/R, matched-lock), a11y (tile state in **label text** not colour alone, matched disabled, progressbar, AA both schemes), controller (select→match locks + persists good, wrong clears, complete+finalize, empty deck). Golden: 5 baselines (playing/selected/correct/almost light + playing dark), `npm run golden` = **match 0.00%** (44/44). `npm test` 527/527; lint clean (0.12 guard active); tsc clean for the slice. **All four non-TTS study modes now run** (match/guess/recall/fill; review 6.1 stays DEP-TTS-gated). No new WBS unblock. 6.2 → Implemented; counts 45 Implemented / 7 Specified / 15 Blocked |
| ca4c6f4 | 2026-07-13 | 7.2 | WBS 7.2 Fill-mode slice (session stage 5): `src/features/session/ui/` — `FillModeScreen` (prop-driven: progress + meaning prompt + a `TextInput` for the term, wrapped in `KeyboardAvoidingView` so **Check stays reachable over the keyboard**; Help/Check controls; on wrong, a per-character **`CharCompare`** diff — each position green if it matches, red if not, spelled out for screen readers — plus the correct answer and accept/retry) + `use-fill-mode` session-play controller + container. Reuses the 7.1 pattern: Check compares the `normalize`d answer (trim+lowercase+collapse) and persists via `recordAnswer` (correct→`good`, wrong→`again`); "I was right" grades `good` (typo override) + advances, Retry re-answers, Help reveals a length+first-char hint; finalizes on completion. Route `src/app/session/fill.tsx` (live container; DB-free `?state=`/`?theme=` preview). Tests (26): screen state matrix + interactions (type/check/help/next/accept/retry), a11y (input + char-compare labels spelling typed-vs-correct, progressbar, AA both schemes), controller (correct→good, wrong→again + answer, hint, retry, accept→good+advance, complete+finalize, empty deck). Golden: 5 baselines (waiting/hint/correct/wrong light + wrong dark), `npm run golden` = **match 0.00%** (39/39). `npm test` 499/499; lint clean (0.12 guard active); tsc clean for the slice. Three of the four non-TTS study modes now run (match 6.2 remains; review 6.1 is DEP-TTS-gated). **Unblocks 7.4 Study-result** (its deps 7.1+7.2 now met) → Specified. 7.2 → Implemented; counts 44 Implemented / 8 Specified / 15 Blocked |
| fcbaeb0 | 2026-07-13 | 6.3 | WBS 6.3 Guess-mode slice (session stage 3): `src/features/session/ui/` — `GuessModeScreen` (prop-driven: progress + prompt + 5 meaning options; on pick, feedback marks the correct option **green ✓** and a wrong pick **red ✕** — colour **and** icon so the cue never relies on colour alone; long meanings wrap without clipping; Continue advances; round-complete shows the score) + `use-guess-mode` session-play controller + pure deterministic `guess-options` builder (correct + up to 4 distractor meanings at a stable per-card index) + container. Reuses the 7.1 session-play pattern (start→pick persists via `recordAnswer` correct→`good`/wrong→`again`→continue→`finalizeSessionUseCase`). Extracted shared `study-chrome.tsx` (ProgressHeader/StudyPromptCard/FeedbackNote/RoundComplete) + shared test fakes `src/shared/testing/session-fakes.ts`. Route `src/app/session/guess.tsx` (live container; DB-free `?state=`/`?theme=` preview). Tests (29): pure option-builder (deterministic placement, distractors, single-card, out-of-range), screen state matrix + interactions (pick, disabled-after-feedback, continue), a11y (feedback in **label text** not colour alone, disabled state, progressbar, AA both schemes), controller (load options + start, correct scores + persists good, wrong persists again, continue steps, complete+finalize, empty deck). Golden: 5 baselines (waiting/correct/wrong/long-text light + waiting dark), `npm run golden` = **match 0.00%** (34/34). `npm test` 473/473; lint clean (0.12 guard active); tsc clean for the slice. No new WBS unblock. 6.3 → Implemented; counts 43 Implemented / 8 Specified / 16 Blocked |
| 1705f42 | 2026-07-13 | 7.1 | WBS 7.1 Recall-mode slice (session stage 4 — first study-mode screen): `src/features/session/ui/` — `RecallModeScreen` (prop-driven: progress header with `progressbar` value + big prompt card + reveal/meaning panel that announces hidden vs revealed + equal-width **Forgot** (outline-danger) / **Got it** (primary) grade pair + `forgot`/`remembered` feedback notes + round-complete) + `use-recall-mode` **session-play controller** + container. The controller runs a single-mode round: `startSession` (5.1) → step card-by-card → each grade persists via `recordAnswer` (5.2, transactional attempt+SRS: **forgot→`again`** re-queues the card this round, **remembered→`good`** advances) → `finalizeSessionUseCase` when every card is remembered; empty deck completes immediately. Route `src/app/session/recall.tsx` (live container; DB-free `?state=`/`?theme=` preview). Tests (23): screen state matrix + interactions, a11y (progressbar value, meaning hidden/revealed labels, AA both schemes), controller (start→reveal→grade persists attempt+SRS→advance, complete+finalize, forgot re-queue, empty-deck). Golden: 5 baselines (before-reveal/revealed/remembered/complete light + revealed dark), `npm run golden` = **match 0.00%** (29/29). `npm test` 444/444; lint clean (0.12 guard active); tsc clean for the slice. Establishes the session-play pattern for the other study modes (6.2/6.3/7.2). No new WBS unblock. 7.1 → Implemented; counts 42 Implemented / 9 Specified / 16 Blocked |
| 9e2aa3c | 2026-07-13 | 4.6 | WBS 4.6 Search slice + index: `src/features/search/data/` — `searchLibrary(db, query, now, limit)` (case-insensitive LIKE over card term + meaning, joined to the deck for a title and LEFT-joined to `srs_state` for a per-hit status new/due/mastered; escapes `%`/`_`/`\` so user wildcards are literal; blank → []) + `recent-searches` (recent history over the `app_setting` store — most-recent-first, deduped case-insensitively, capped 8, corrupt-tolerant). `src/features/search/ui/` — `SearchScreen` (search-dock app bar + filter chips All/Due/Mastered/New + result rows with **solid** status badges + recent list w/ use/remove/clear + skeleton + no-results that quotes the query) + **debounced** `use-search` controller (derives phase so the effect never sets state synchronously; records the query in recent when a hit is opened) + container. Route `src/app/search.tsx` (live container; DB-free `?state=`/`?theme=` preview; opens a hit → `/card/[id]`). **A11y finding:** MxBadge `soft` fails AA in dark (onSuccessSoft 1.68:1 / onWarningSoft 1.41:1) → used **solid** status badges (onSuccess/success ≈ 5.2–7.3); filed task_a92bf890 to fix the dark on*Soft tokens. Tests (39): data (search-query real-SQLite incl. status + LIKE-escaping, recent-history), screen state matrix + interactions, a11y (labels/progressbar/AA both schemes), controller (debounce → results/no-results, recent load + record-on-open, remove/clear). Golden: 5 baselines (empty-recent/results/filtered/no-results light + results dark), `npm run golden` = **match 0.00%** (24/24). `npm test` 421/421; lint clean (0.12 guard active); tsc clean for the slice. **Unblocks 3.4 Library** (P0 — its last missing dep) → Specified. 4.6 → Implemented; counts 41 Implemented / 10 Specified / 16 Blocked |
| c253e3f | 2026-07-13 | 4.5 | WBS 4.5 Deck-settings slice: `src/features/library/ui/` — `DeckSettingsScreen`, an overlay-driven deck-lifecycle manager over a minimal backdrop: **action sheet** (Rename/Move/Export/Reset/Delete) → **rename dialog** (validated), **move sheet** (radio list of language pairs, current disabled), and danger **reset**/**delete** confirm dialogs (busy + failure states). Overlays render in a React Native **`Modal`** (portaled full-screen — fixes the fragile absolute-overlay-in-ScrollView pattern that collapses when the backdrop is short; filed task_4d0cad6d to migrate the languages/mode-picker overlays too). Controller `use-deck-settings` + container wire the writes. Domain: pure `moveDeck` + `moveDeckUseCase` (validated language-pair move). Data: transactional `resetDeckProgress(db, cardIds)` (`src/features/session/data/reset-deck-progress.ts`) clears SRS state + attempts in one `tx` — with an atomic + **rollback** test (injected failing db → neither table half-cleared). Route `src/app/deck/[deckId]/settings.tsx` (live container; DB-free `?state=`/`?theme=` overlay preview; delete → `/(tabs)/library`). Tests (46 across the slice): overlay state matrix + interactions (open each overlay, rename save/validation, move current-disabled, danger confirms + failure), a11y (roles/labels/radio+disabled state/AA incl. danger both schemes), controller wiring, domain move, data reset+rollback. Golden: 6 baselines (5 overlays light + action-sheet dark), `npm run golden` = **match 0.00%** (18/18). `npm test` 382/382; lint clean (0.12 guard active); tsc clean for the slice. No new WBS unblock. 4.5 → Implemented; counts 40 Implemented / 10 Specified / 17 Blocked |
| 108951d | 2026-07-13 | 5.4 | WBS 5.4 Mode-picker slice (first study-flow screen): `src/features/session/ui/` — `ModePickerScreen` (prop-driven: success-tinted Card-source card → **bottom-sheet** scope selector with 3 radio sources; 5 accent-tiled mode options mapping to the 5.1 session stages; **not-enough guard** — info banner + Add-words CTA disables/dims the modes below the threshold; loading `Counting…`; footer) + `use-mode-picker` controller + `ModePickerContainer`. Controller counts words per scope from existing ports (no schema change): all=`cards.countByDeck`/`listByDeck`, srs=`srs.dueCards`, unlearned=cards with no `srs.list()` state. Pure `mode-picker-model.ts` (mode/scope catalogs, `MIN_WORDS=4`). Route `src/app/session/mode-picker.tsx` (live container; DB-free `?state=`/`?theme=` fixture preview; routes a chosen mode to `/session/<mode>?deckId=&scope=`). Tests (25): screen state matrix + interactions (open sheet, select source, start mode, guard disables), a11y (roles/labels/radio-state/AA both schemes), controller (all/srs/unlearned counts + scope switch). Golden: 4 baselines `mode-picker--{default,scope-dropdown,not-enough}--light` + `--default--dark`, `npm run golden` = **match 0.00%** (12/12). `npm test` 346/346; lint clean (0.12 guard active); tsc clean for the slice. No new WBS unblock. 5.4 → Implemented; counts 39 Implemented / 11 Specified / 17 Blocked |
| 0577b88 | 2026-07-13 | 3.6 | WBS 3.6 Deck-content-choice slice: `src/features/library/ui/` — `DeckContentChoiceScreen` (prop-driven: name field + "How do you want to organise it?" heading + two accent-tiled choice cards `Organise with subdecks`/`Add cards directly` + tertiary Import link; per-card busy spinner; blank-name validation inline; failure → **accessible bordered** error banner) + `use-deck-content-choice` controller (loads the deck name, persists via the new use case) + `DeckContentChoiceContainer` (resolves the deck repo, routes on success). Domain: pure `reorganiseDeck(deck, organisation, updatedAt)` + `setDeckContentUseCase` (rename+organise+save atomically; blank title short-circuits; not-found surfaced) in `src/features/library/domain/{deck,use-cases}.ts`. Route `src/app/deck/[deckId]/content.tsx` (live container; DB-free `?state=`/`?theme=` fixture preview for the golden; routes subdecks→`/deck/[id]`, cards→`/deck/[id]/cards`). Tests (26): domain (reorganise/use-case), screen state matrix + choose lifecycle, a11y (roles/labels/AA both schemes), controller (fake-repo wiring). **A11y finding:** dark `errorSoft`/`onErrorSoft` fails AA for normal text (≈1.88:1) — switched the error banner to a bordered `surface` card (`text` on surface + `error` border/icon, AA-safe both schemes); filed task_6d0f5d1c to fix the same latent bug in the Languages banner. Golden: 3 baselines `deck-content-choice--{default,named}--light` + `--default--dark`, `npm run golden` = **match 0.00%** (8/8). `npm test` 321/321; lint clean (0.12 guard active); tsc clean for the slice. No new WBS unblock. 3.6 → Implemented; counts 38 Implemented / 12 Specified / 17 Blocked |
| e814875 | 2026-07-13 | 0.12 | WBS 0.12 Raw visual-value guard: add a scoped block to `eslint.config.js` that fails the build, in `src/features/**` + `src/app/**` (tests + `fixtures.ts` exempt), on (a) raw `#hex`/`rgb()`/`rgba()`/`hsl()` colour literals (`no-restricted-syntax` over `Literal`/`TemplateElement`) and (b) direct imports of the raw token layer `@/design-system/tokens` or the named `tokens`/`Tokens` export (`no-restricted-imports`) — enforcing one-way layering (Token→Component→Screen, ADR 0004); colours must come from `useTheme()`. Runs in `npm run lint`. Verified: guard is clean on the tree and **fires** on a planted violation (hex + rgba + token import → 3 errors); fixed the one pre-existing violation (`src/app/+not-found.tsx` raw `#4b3a8c`/off-scale px → token-driven via `useTheme`). Documented in `docs/design/mobile-ui-construction-contract.md` §4 (off-scale spacing stays review-enforced — a numeric literal is ambiguous to lint). `npm test` 295/295; lint clean; tsc clean for the change. No new WBS unblock (governance guard). 0.12 → Implemented; counts 37 Implemented / 13 Specified / 17 Blocked |
| ff0f6ef | 2026-07-13 | 3.3 | WBS 3.3 Languages slice (first production feature screen): `src/features/languages/ui/` — `LanguagesScreen` (prop-driven presentational: loading · recoverable-error+retry · empty · one · list with deck-count subtitles · **add** form with submit lifecycle validation/failure/success · **remove** confirm dialog), `use-languages` controller (wires 3.1 use-cases + 3.2 repos over `createLibraryRepositories()`, counts decks per pair, subscribes for live refresh), `LanguagesContainer`, and shared view-model + state-matrix fixtures. Route `src/app/settings/languages.tsx` renders the live container, with a DB-free `?state=`/`?theme=` fixture preview for the golden. Add `src/shared/runtime.ts` (production `IdGenerator`/`Clock` impls of the 0.7 ports). Tests (39): `__tests__/{languages-screen (state matrix + add/remove lifecycle), a11y (roles/labels/≥44px/AA contrast light+dark), use-languages (fake-repo wiring)}`. Golden: 4 committed baselines `languages--{list,empty,add}--light.png` + `--list--dark.png`, `npm run golden` = **match 0.00%**. `npm test` 295/295; lint clean; tsc clean for the slice (1 pre-existing unrelated `/explore` error in `src/components/app-tabs.web.tsx`). No new WBS unblock (3.4 also needs 4.6). 3.3 → Implemented; counts 36 Implemented / 14 Specified / 17 Blocked |
| 2505754 | 2026-07-13 | 2.6 | WBS 2.6 Feature quality-harness readiness gate: add `docs/testing/feature-screen-readiness.md` — confirms the three shared harnesses are complete + usable (11.1 test harness + fixtures `src/shared/testing/{fixtures,sqlite-test-db}`; 11.3 a11y helpers `src/shared/testing/a11y.ts` + contract; 11.4 golden `tool/app_golden/`), and states the mandatory per-slice quality contract (state-matrix fixtures + component/interaction tests, a11y roles/labels/targets/contrast, committed visual golden states). Readiness gate only — no feature screen built. **Gate PASSED.** Unblocks the first feature-screen wave 3.3/3.5/3.6/4.3/4.5/4.6/5.3/5.4/6.2/6.3/7.1/7.2 → Specified. 2.6 → Implemented; counts 35 Implemented / 15 Specified / 17 Blocked |
| b7eeb9b | 2026-07-13 | 11.4 | WBS 11.4 Visual regression harness: **approve DEP-GOLDEN** (Playwright chromium screenshots of the Expo web build, pixel-diffed with `pixelmatch`/`pngjs`). Add `tool/app_golden/shoot.mjs` — serves the web export, captures each canonical state at 390×780, diffs vs `baseline/` (naming `<screen>--<state>--<theme>.png`, tolerance 0.1/≤1% pixels), writes `diff/` + `out/report.json`, exits 1 on mismatch; `--update` (re)writes baselines. `npm run golden` / `golden:update` scripts; `tool/app_golden/README.md` (approach + per-slice contract: each screen appends its states to TARGETS). Committed shell baseline `baseline/shell-dashboard--loaded--light.png` (real render: app bar + FAB + Material Symbols bottom nav on the violet canvas); verify run = **match 0.00%**. web-build/out/diff gitignored. Unblocks 2.6 → Specified. 11.4 → Implemented; counts 34 Implemented / 4 Specified / 29 Blocked |
| 05c5caa | 2026-07-13 | 2.5 | WBS 2.5 Shell/theme visual verification: rendered the RN app on web (`expo start --web` via `.claude/launch.json` `app-web`, 375×812) and verified the shell + theme states against the kit's canonical shots. Report `docs/testing/2.5-shell-theme-verification.md` — dashboard shell (app bar "Today" + FAB + 4 roled tabs), theme screen (appearance/accent×7/text-size/preview), **live dark reskin** (canvas → `#141220`, surfaces + primary reskinned via computed styles), correct accent-swatch colours, and roles/labels. Browser raster screenshot timed out on the live server (renderer capture limitation, no console errors) → verified via the a11y tree + `getComputedStyle` (stronger for token application; pixel golden diffs are 11.4). 0 unaccepted defects; 2 accepted minors flagged (RN-web `shadow*` deprecation → web `boxShadow` follow-up; screenshot capture). No new unblock (11.4 also needs DEP-GOLDEN). 2.5 → Implemented; counts 33 Implemented / 3 Specified / 31 Blocked |
| 0ce7f75 | 2026-07-13 | 11.3 | WBS 11.3 Accessibility tooling baseline: add `src/shared/testing/a11y.ts` (WCAG contrast checker — `parseColor` hex+rgba, `compositeOver` alpha, `relativeLuminance`, `contrastRatio`, `meetsContrastAA`; `MIN_TOUCH_TARGET`; `flatStyle`) + `docs/testing/accessibility-contract.md` (role/label convention, ≥44px target rule + hitSlop, font-scale rule, contrast approach, per-slice a11y ownership). `src/shared/testing/__tests__/a11y.test.ts` locks the token AA matrix in **both** schemes (text/secondary/tertiary on bg/surface, on-* on solids at 4.5; semantic soft tints back bold badges → AA-large 3:1). Finding: light `onWarningSoft` on `warningSoft` = 4.27:1 (under 4.5 for normal text; fine for the bold-badge usage) — flagged for a design pass. 33 new tests; `npm test` 261/261 + `tsc` + `expo lint` clean. No new unblock (2.6 also needs 11.4). 11.3 → Implemented; counts 32 Implemented / 4 Specified / 31 Blocked |
| acf29fe | 2026-07-13 | 2.4 | WBS 2.4 Shell/theme state matrix + tests: add `src/design-system/components/__tests__/shell-theme-matrix.test.tsx` — the full theme matrix (mode light/dark × accent brand/green/coral/cyan × text-scale 0.9/1/1.15, each asserted via a `useTheme` probe) and app-bar state matrix (root/nested/search/selection/modal, each with app-bar + scroll-anchor testIDs, light + dark), plus interactions: **scroll → app-bar elevates** (transparent→surface), search dock hosted in the bar, and the **theme screen reskinning live** on appearance change. Add an AppScreen `main` slot (forwarded to the app bar for search) + a `${node}/scroll` testID anchor. 17 new tests; `npm test` 228/228 + `tsc` + `expo lint` clean. Unblocks 2.5 + 11.3 → Specified. 2.4 → Implemented; counts 31 Implemented / 5 Specified / 31 Blocked |
| 4e567ba | 2026-07-13 | 2.3 | WBS 2.3 Theme screen + persistence: migration v4 `app_setting` (key/value); `src/features/settings/data/` (SqliteSettingsRepository get/set + typed `loadThemeSettings`/`saveThemeSettings`, corrupt→default). Extend the theme: `resolveTheme(scheme,{accent,textScale})` overrides `color.accent`/`accentSoft` from the palette + scales composed font sizes; ThemeProvider gains `accent`/`textScale` state + `onSettingsChange`; new `useThemeSettings` hook. `PersistedThemeProvider` (`features/settings/ui`) hydrates from the DB on launch + persists changes, mounted at the root in place of `ThemeProvider`. Theme screen (`ThemeScreen`, route `settings/theme.tsx`): appearance (light/dark/system segmented), accent swatches (labeled, ≥48px), text-size — all **live-previewed** (the screen reskins from the theme). Add `metro.config.js` (register `.wasm` so expo-sqlite bundles for web). 12 new tests (settings round-trip across "restart", accent/scale overrides, screen smoke). `npm test` 211/211 + `tsc` + `expo lint` + `expo export --platform web` clean. Unblocks 2.4 → Specified. 2.3 → Implemented; counts 30 Implemented / 4 Specified / 33 Blocked |
| 1045b8f | 2026-07-13 | 2.2 | WBS 2.2 Contextual app-bar variants: extend `MxContextualAppBar` with a `main` slot (holds a search dock for the search variant), a selection `count` (→ "N selected"), a tinted selection background, and a `header` a11y role; titles already truncate to one line (ellipsis). All 8 states compose from the primitives — root-top, root-scrolled, root-unread (notification action + MxBadge), nested (back + context breadcrumb + title), nested-overflow (+ more_vert), search (MxSearchDock flat), selection (count + delete on tint), modal (centered title between close + Save). 8 new render tests (full state-matrix/interaction suite owned by 2.4). `npm test` 199/199 + `tsc` + `expo lint` clean. No new unblock (2.4 also needs 2.3). 2.2 → Implemented; counts 29 Implemented / 4 Specified / 34 Blocked |
| 2506c42 | 2026-07-13 | 2.1 | WBS 2.1 App shell + router layout: replace the default Expo Router tab bar with the MemoX bottom nav — `src/app/(tabs)/_layout.tsx` renders `MxBottomNav` as a custom `tabBar` (Today/Library/Stats/Profile, icons home/library_music/bar_chart/person, driven by navigation state, safe-area bottom inset). Add the `AppScreen` shell (`src/design-system/components/AppScreen.tsx`): SafeAreaView top + a fixed root `MxContextualAppBar` that **elevates on scroll** (scrolled state from ScrollView offset) + screen padding/section gap + FAB slot. Wire the 4 tab screens to render their placeholder inside `AppScreen` (dashboard has a FAB). Render smoke test; `npm test` 191/191 + `tsc` + `expo lint` + `expo export --platform web` (all 26 routes bundle) clean. Unblocks 2.2 → Specified. 2.1 → Implemented; counts 28 Implemented / 5 Specified / 34 Blocked |
| dd3a736 | 2026-07-13 | 1.8 | WBS 1.8 Fixtures + accessibility + parity tests: add `src/design-system/components/__tests__/{a11y,parity}.test.tsx` — testID presence for all 18 primitives; a11y roles/states (button/link/switch+checked/chip+selected/segmented+bottom-nav tabs); target-size ≥44px (MxButton minHeight, MxFab height, icon-button/switch via touch-min token); token binding (MxCard surface/primary, MxBadge error, **MxList space-3 gap** + override); **no raw `#hex` colour literals** in any Mx* component source (fs scan); and the icon-adapter **missing-glyph fallback** (unknown name → deterministic FALLBACK_ICON glyph) + known-glyph render. 18 new tests; `npm test` 189/189 + `tsc` + `expo lint` clean. Completes design-system section 1. No new unblock (11.3 also needs 2.4). 1.8 → Implemented; counts 27 Implemented / 5 Specified / 35 Blocked |
| cbafafe | 2026-07-13 | 1.7 | WBS 1.7 Core input/action primitives: add `src/design-system/components/core/` — the eight RN `Mx*` core components (ADR 0004): MxButton (`btn` — primary/secondary/outline/ghost + danger + block + icon/trailingIcon + sm, ≥48px), MxLink (`link` — accent, default chevron_right, size), MxTextField (`field` — label/helper/error labelled input, error border+message), MxChip (`chip` — ghost/accent + selected), MxBadge (`badge` — tone/soft/dot), MxSwitch (`switch` — track+thumb toggle, ≥48px hit, ariaLabel), MxSegmentedControl (`segmented` — active raised segment, block, per-segment icon), MxAvatar (`avatar` — initials/image, size/variant/ring). Token-driven via `useTheme`, use the 1.4 Icon, `node`→`testID`. Render smoke test (8 behaviours: press/disabled/error/toggle/segment/initials); full a11y/parity owned by 1.8. `npm test` 171/171 + `tsc` + `expo lint` clean. **All 18 primitives complete.** Unblocks 1.8/2.1/2.3 → Specified. 1.7 → Implemented; counts 26 Implemented / 6 Specified / 35 Blocked |
| 5f5c34e | 2026-07-13 | 1.6 | WBS 1.6 Navigation primitives: add `src/design-system/components/navigation/` — the four RN `Mx*` navigation components (ADR 0004): MxBottomNav (`bottom-nav` — 80px surface bar, per-item icon+label, active gets a primary-soft pill, `value`/`onChange`, tab a11y + per-item testID), MxFab (`fab` — 56px brand-filled elevated, extended with `label`, `round`, accent variant), MxSearchDock (`search-dock` — pill TextInput with leading search glyph, `focused` ring, `flat`, `trailing` slot), MxIconButton (`icon-btn` — round ghost/filled/primary, `sm` keeps a ≥48px hit area via hitSlop, disabled). Token-driven via `useTheme`, uses the 1.4 Icon, `node`→`testID`. Render smoke test (press/change behaviours); full a11y/parity owned by 1.8. `npm test` 163/163 + `tsc` + `expo lint` clean. No new unblock (2.x/1.8 also need 1.7). 1.6 → Implemented; counts 25 Implemented / 4 Specified / 38 Blocked |
| 5dcee46 | 2026-07-13 | 1.5 | WBS 1.5 Surface primitives: add `src/design-system/components/surfaces/` — the six RN `Mx*` surface components mapped from the kit per ADR 0004: MxScaffold (`app` shell — SafeAreaView + scrolling body with screen padding/section gap + bottom-nav/fab slots, `flush`), MxContextualAppBar (`cappbar` — 56px, leading/main(context+title)/actions, top↔scrolled state, variants incl. modal-centered), MxCard (`card` — elevated/flat/muted/primary/primary-soft + interactive Pressable + padding), MxSectionHeader (`section-head` — title/caption + action link), MxIconTile (`icon-tile` — tone/size/solid, uses the 1.4 Icon), MxList (token-gap stack). All token-driven via `useTheme`, `node`→`testID`, frozen names/variants. Render smoke test (6 components mount under theme); full a11y/parity suite owned by 1.8. `npm test` 158/158 + `tsc` + `expo lint` clean. No new unblock (2.x/1.8 also need 1.6/1.7). 1.5 → Implemented; counts 24 Implemented / 5 Specified / 38 Blocked |
| 5732082 | 2026-07-13 | 1.4 | WBS 1.4 Icon adapter: **approve DEP-ICON-FONT** (register → Approved; Material Symbols Rounded, Apache-2.0, sourced from google/material-design-icons, subset via pyftsubset to the 119 kit icons → `assets/fonts/MaterialSymbolsRounded.ttf` ~246 KB, bundled, no CDN). Add `src/design-system/icons/` — generated `glyphs.ts` (name→PUA-codepoint map + `FALLBACK_ICON`), `Icon.tsx` (renders a glyph by codepoint in the icon font at icon-size tokens; deterministic missing-glyph fallback; a11y image/decorative; `node`→`testID`; no font-scaling), barrel — and register the font in `src/design-system/fonts/`. Glyph rendering by codepoint (ligatures unreliable in RN Text); no SF Symbols/emoji. Full adapter render suite owned by 1.8; glyph-map sanity test here. `npm test` 152/152 + `tsc` + `expo lint` + `expo export --platform web` (font bundles) clean. Unblocks 1.5/1.6/1.7 → Specified. 1.4 → Implemented; counts 23 Implemented / 6 Specified / 38 Blocked |
| d275f2c | 2026-07-13 | 11.2 | WBS 11.2 Repository + migration tests: add `src/db/__tests__/migrations-integration.test.ts` — real-SQLite integration proving the whole persistence contract end-to-end: the migration chain applies v0→v1→v2→v3 **incrementally** (vN→vN+1, asserting tables/columns + `PRAGMA user_version` at each step), re-running the registry is a no-op (forward-only, idempotent), a **multi-table** transactional write (deck+card) rolls back atomically on an FK violation, and the DB round-trips writes (source-of-truth). Complements the per-slice repo rollback tests (3.2/4.2/5.2) and the runner unit tests (0.5). 6 new tests; `npm test` 149/149 + `tsc` + `expo lint` clean. No new unblock (leaf). 11.2 → Implemented; counts 22 Implemented / 3 Specified / 42 Blocked |
| 900a269 | 2026-07-13 | 11.1 | WBS 11.1 Test harness baseline + quality contract: add `docs/testing/quality-contract.md` (test pyramid + tiers with in-repo examples; `__tests__`/`*.test` folder+naming convention; shared-fixtures home `src/shared/testing/`; `npm test` + coverage/command contract; the **Vertical Slice Quality Ownership** rule — every slice ships its own tier-appropriate tests in its own PR). Add shared fixtures `src/shared/testing/fixtures.ts` (`sequentialIds`/`fixedClock`/`tickingClock`, matching the IdGenerator/Clock ports) + test; document `sqlite-test-db.ts` as the shared repository-test harness. Baseline proven by the 20 merged slices. `npm test` 143/143 + `tsc` + `expo lint` clean. No new unblock (2.6 also needs 11.3/11.4). 11.1 → Implemented; counts 21 Implemented / 4 Specified / 42 Blocked |
| 66c2154 | 2026-07-13 | 5.2 | WBS 5.2 Session + progress persistence: migration `src/db/migrations/003-sessions.ts` (srs_state/session/attempt tables; session stores the ordered `card_ids` as JSON for lossless resume; FK CASCADE; registered v3). `src/features/session/data/` — Sqlite Session/Attempt/SrsState repositories (implement the 5.1 ports incl. activeForDeck/listBySession/dueCards; upsert; keyed-by-card SRS), mappers, and `persistAnswer` (writes attempt + SRS in ONE transaction). 14 new real-SQLite tests: session round-trip + card_ids, activeForDeck (excludes finalized), **lossless resume** (rebuild sessionProgress from persisted attempts), SRS upsert + due-window query, and `persistAnswer` **atomic commit + rollback** (dup attempt id → SRS advance reverts). `npm test` 140/140 + `tsc` + `expo lint` clean. Completes the persistence layer; no new unblock (dependents need 2.6/primitives). 5.2 → Implemented; counts 20 Implemented / 5 Specified / 42 Blocked |
| 8c7cea6 | 2026-07-13 | 4.2 | WBS 4.2 Flashcard persistence: migration `src/db/migrations/002-flashcards.ts` (card + card_translation tables; FK CASCADE/SET NULL; `deck.card_count` column maintained by AFTER INSERT/DELETE/UPDATE-of-deck_id triggers so the count stays consistent transactionally; registered v2). `src/features/flashcards/data/` — Sqlite Card repository (implements 4.1 port incl. new `countByDeck`; tags stored as JSON) + Sqlite CardTranslation repository (listByCard); mappers (tags JSON parse, malformed→`[]`); factory. 13 new real-SQLite tests: tags round-trip, listByDeck, upsert-no-double-count, count trigger maintenance, **transactional rollback keeps count consistent**, FK cascade (deck→cards, card→translations). `npm test` 126/126 + `tsc` + `expo lint` clean. Unblocks 5.2 → Specified. **Ledger correction:** re-classified 5.2/11.1/11.2 Blocked→Specified (deps met at 0.5/0.13). 4.2 → Implemented; counts 19 Implemented / 6 Specified / 42 Blocked |
| 90302db | 2026-07-13 | 3.2 | WBS 3.2 Content persistence: migration `src/db/migrations/001-content.ts` (language_pair/deck/subdeck tables, FK ON DELETE CASCADE, organisation CHECK, indexes; registered v1); `src/db/sql.ts` (`SqlDatabase` port + `expoSqlDatabase` adapter + `getSqlDatabase`); `src/db/change-signal.ts`; `src/features/library/data/` (Sqlite Deck/Subdeck/LanguagePair repositories over the port implementing the 3.1 ports — upsert + typed not-found + change signal — mappers, factory). **Real-SQLite test harness** `src/shared/testing/sqlite-test-db.ts` (better-sqlite3 devDep; runs the real migrations; sql.js was rejected — fails under jest). 11 new tests: round-trip/upsert, listByDeck ordering, FK cascade, subscribe, and transactional multi-write **rollback** (bad CHECK mid-tx → nothing persists). `npm test` 113/113 + `tsc` + `expo lint` clean. Unblocks 4.2 → Specified. 3.2 → Implemented; counts 18 Implemented / 4 Specified / 45 Blocked |
| 9a6d350 | 2026-07-13 | 0.5 | WBS 0.5 Migration strategy: add `src/db/migrations/` — `types.ts` (`Migration` + `MigrationDb` port), `runner.ts` (`validateMigrations` forward-only contiguous-1..N check + `runMigrations` applying each pending step in its own transaction that bumps `PRAGMA user_version`, rollback-on-throw, typed Result), `index.ts` (empty registry slices append to). Add `src/db/client.ts` (`getDb` opens `memox.db`, WAL + FK on, runs migrations once memoized; `tx(work)` transaction helper via expo-sqlite `withTransactionAsync`, ADR 0005). 10 unit tests for the runner over an in-memory fake (in-order apply, no-op when current, vN→vN+1 tail, rollback stops at last good version, bad-registry rejection). `npm test` 102/102 + `tsc` + `expo lint` clean. Unblocks 3.2 → Specified. 0.5 → Implemented; counts 17 Implemented / 4 Specified / 46 Blocked |
| f7b220c | 2026-07-13 | 5.1 | WBS 5.1 Study/session domain + SRS: add `src/features/session/domain/` — `stage.ts` (5 session stages review→match→guess→recall→fill + ordering), `srs.ts` (deterministic SM-2 scheduler over srs_state: initial/isDue/schedule; grade→interval/ease/lapse; relearning on `again`, ease floor 1.3), `session.ts` (Session/Attempt models, `makeSession`, `sessionProgress` stage-major state machine, `finalizeSession`; resume = recompute from attempt count), `ports.ts` (Session/Attempt/SrsState repositories with activeForDeck/listBySession/dueCards), `use-cases.ts` (startSession, recordAnswer = attempt+SRS update, resumeSession, finalizeSessionUseCase, getDueCards). Pure-TS, Result-returning, no UI/DB. 25 new unit tests (SRS determinism/relearn, stage ordering, state machine, resume, use cases over fakes). `npm test` 92/92 + `tsc` + `expo lint` clean. Completes the domain layer; no new unblock (5.2 needs 0.5). 5.1 → Implemented; counts 16 Implemented / 4 Specified / 47 Blocked |
| f9f2627 | 2026-07-13 | 4.1 | WBS 4.1 Flashcard domain: add `src/features/flashcards/domain/` — `card.ts` (Card entity, `makeCard`/`editCard`, `normalizeTags`, term/meaning/tags/audioRef modeled + length/required validation), `card-translation.ts` (CardTranslation + `makeCardTranslation`), `duplicate.ts` (`normalizeTerm` + `findDuplicate` with `exceptId`), `ports.ts` (CardRepository/CardTranslationRepository with deck/card-scoped reads), `use-cases.ts` (createCard rejecting same-deck duplicates, editCardUseCase, deleteCard, addTranslation). Pure-TS, Result-returning, no UI/DB. 17 new unit tests (entity/validation + duplicate detection + use cases over fakes). `npm test` 67/67 + `tsc` + `expo lint` clean. Unblocks 5.1 → Specified. 4.1 → Implemented; counts 15 Implemented / 5 Specified / 47 Blocked |
| dad0b98 | 2026-07-13 | 3.1 | WBS 3.1 Deck/subdeck/language domain: add `src/features/library/domain/` (Deck + Subdeck entities with validating factories; structural invariants `isAncestor`/`moveSubdeck`/`assertNotOrphan` — no orphan, no cross-deck parent, no cycle; DeckRepository/SubdeckRepository ports; use cases createDeck/renameDeckUseCase/deleteDeck/createSubdeck/moveSubdeckUseCase/deleteSubdeck) and `src/features/languages/domain/` (LanguagePair entity + port + addLanguagePair/removeLanguagePair). Add `src/shared/contracts/services.ts` (IdGenerator/Clock ports for pure domain). Pure-TS, Result-returning, no UI/DB imports. 19 new unit tests (entities + invariants + use cases over in-memory fakes). `npm test` 50/50 + `tsc` + `expo lint` clean. Unblocks 4.1 → Specified. 3.1 → Implemented; counts 14 Implemented / 5 Specified / 48 Blocked |
| c8016ed | 2026-07-13 | 0.7 | WBS 0.7 Repository/use-case contracts: add `src/shared/contracts/` — `use-case.ts` (`UseCase<I,O>`/`UseCaseFn`/`SyncUseCase`, all returning `Result`), `repository.ts` (generic `Repository<T,ID>` with Result-returning reads, `Transactional` `tx(work)` per ADR 0005, `Observable` change-signal per ADR 0003), barrel; re-exported from `src/shared`. Engine-independent, no UI/DB imports. Conformance test implements a fake repo + use case proving Result composition + not-found/conflict/observe. `npm test` 26/26 + `tsc` + `expo lint` clean. Unblocks 3.1 → Specified. 0.7 → Implemented; counts 13 Implemented / 5 Specified / 49 Blocked |
| 63ec1c9 | 2026-07-13 | 0.6 | WBS 0.6 Error/result model: add `src/shared/result.ts` (`Result<T,E>` discriminated union + `ok`/`err`/`isOk`/`isErr`/`map`/`mapError`/`flatMap`/`unwrapOr`), `src/shared/errors.ts` (`AppError` union: validation/not-found/conflict/storage/unexpected + constructors + `UserFacingError` + total `toUserFacingError` mapping where storage/unexpected are retryable), barrel `src/shared/index.ts`. Pure shared kernel (no React/DB). 12 unit tests cover the combinators + the mapping (every kind → non-empty title/message; retryable matches the recoverable-error contract). `npm test` 22/22 + `tsc` + `expo lint` clean. Unblocks 0.7 → Specified. 0.6 → Implemented; counts 12 Implemented / 5 Specified / 50 Blocked |
| 9cab3bd | 2026-07-13 | 0.13 | WBS 0.13 Production test harness: install the approved DEP-TEST stack (`jest-expo ~57`, `@testing-library/react-native ^13` with its `react-test-renderer@19.2.3` engine, `jest`+`@types/jest` peers — RNTL pinned to v13 because v14's new renderer returns an empty tree under jest-expo 57); add `jest.config.js` (jest-expo preset + transformIgnorePatterns) + `jest.setup.js`; `npm test`=`jest`; `types: ["node","jest"]` in tsconfig so tests typecheck; three samples pass (unit: token/theme resolution; RN component: `RoutePlaceholder` via RNTL; repository/integration: transactional-rollback contract per ADR 0005); wire `.github/workflows/test.yml` (jest on `src/**`/config/deps). `npm test` 10/10 + `tsc` + `expo lint` clean. Unblocks 0.6 → Specified. 0.13 → Implemented; counts 11 Implemented / 5 Specified / 51 Blocked |
| 0a0d1ef | 2026-07-13 | 1.3 | WBS 1.3 Plus Jakarta Sans fonts: copy the variable TTF to `assets/fonts/PlusJakartaSans.ttf`; add `src/design-system/fonts/index.ts` (`useMemoxFonts` wrapping expo-font's `useFonts`, registered under the frozen family name `Plus Jakarta Sans` so `theme.font.text` resolves the real font); splash-gate render in `src/app/_layout.tsx` (preventAutoHide → hideAsync on load/error → `return null` until loaded) so no system-font flash. Verified: `tsc` + `expo lint` clean, and `expo export --platform web` bundles all 26 routes + the font asset (exit 0). Weight-render parity verifies when components use the font (1.5+/2.x). 1.3 → Implemented; counts 10 Implemented / 5 Specified / 52 Blocked |
| 5b5d1e3 | 2026-07-13 | 1.2 | WBS 1.2 Theme provider + light/dark: add `src/design-system/theme/` (`resolveTheme(scheme)` → active `Theme` from tokens; `ThemeProvider` reads system scheme via `@/hooks/use-color-scheme` + in-memory `mode` override; `useTheme`/`useThemeMode` hooks; `font.text()` composer since the kit has no named type roles) + top barrel `src/design-system/index.ts`; mount `ThemeProvider` in `src/app/_layout.tsx`. No component branches on scheme (ADR 0004); override persistence deferred to 6.x/10.1. `tsc` + `expo lint` clean. 1.2 → Implemented; counts 9 Implemented / 6 Specified / 52 Blocked |
| 181e8d9 | 2026-07-13 | 1.1 | WBS 1.1 Tokens → TypeScript: add `src/design-system/tokens/` (colors light+dark, spacing+layout, radius, typography, elevation, motion, size/icon-size/stroke/opacity + barrel `index.ts` exposing a frozen `tokens` object). Verbatim from `tokens/*.css`, no value drift; role names frozen; RN-native shadow mapping for elevation (webShadow kept for parity). `tsc --noEmit` + `expo lint` clean. 1.1 → Implemented; counts 8 Implemented / 7 Specified / 52 Blocked |
| dd962fe | 2026-07-13 | 0.11 | WBS 0.11 `data-mx-node` → `testID` mapping: add `docs/adr/0006-testid-mapping.md` — deterministic rule carrying each kit `data-mx-node` id (the `node` prop, `"<screen>/<part>"` slug) onto the RN node as `testID` 1:1 (R1–R5: identity carry-through, single `node` input, one id→one host node, frozen slug shape, coverage), kept distinct from `accessibilityLabel`. 0.11 → Implemented; counts 7 Implemented / 8 Specified / 52 Blocked |
| e67934a | 2026-07-13 | 0.8 | WBS 0.8 Test framework decision: **DEP-TEST approved** (register → Approved; `jest-expo` + `@testing-library/react-native`). Decision only — no install/config here (that is 0.13). Unblocks 0.13 → Specified. 0.8 → Implemented; counts 6 Implemented / 9 Specified / 52 Blocked |
| 44d21ab | 2026-07-13 | 0.4 | WBS 0.4 Local database decision: **DEP-DB approved** (register → Approved); add `expo-sqlite ~57.0.0` (config plugin in `app.json`); `docs/adr/0005-db-schema.md` (offline-first source-of-truth, access-layer contract with `tx()` transaction helper, 10-table schema outline). Unblocks 0.5 → Specified. 0.4 → Implemented; counts 5 Implemented / 8 Specified / 54 Blocked |
| 86e8dd3 | 2026-07-13 | 0.10 | WBS 0.10 UI Kit → React Native mapping: add `docs/adr/0004-uikit-rn-mapping.md` — layering rule, token-CSS→TS-theme mapping, web-idiom→RN-idiom table (onClick→onPress, className→theme styles, material-symbols→Icon, data-mx-node→testID), and a mapping table for all 18 `Mx*` primitives (base class + variants→props, names frozen). 0.10 → Implemented; counts 4 Implemented / 7 Specified / 56 Blocked |
| 9d767a0 | 2026-07-13 | 0.3 | WBS 0.3 State-management decision: add `docs/adr/0003-state.md` — no global store lib; DB-backed state via a repository-hooks layer over `useSyncExternalStore`, focused Contexts for theme/session/db-readiness, local `useState`/`useReducer` for UI/forms; rejected Redux/Zustand/Jotai/React-Query/MobX; 0 new dependencies. 0.3 → Implemented; counts 3 Implemented / 8 Specified / 56 Blocked |
| 770c84f | 2026-07-13 | 0.2 | WBS 0.2 Expo Router strategy: add `docs/adr/0002-routing.md` (screen→route map for all 25 business screens + back/deep-link notes) and the `src/app/**` route tree — `(tabs)` group (Today/Library/Stats/Profile), `session`/`settings`/`deck/[deckId]` stacks, `card`/`search`/`player` modals, 26 placeholder route files + `+not-found`, retire starter `index`/`explore`. `tsc --noEmit` + `expo lint` pass. 0.2 → Implemented; counts 2 Implemented / 9 Specified / 56 Blocked |
| b6893dc | 2026-07-13 | 0.1 | **Implementation begins.** WBS 0.1 Production folder architecture: add `docs/adr/0001-architecture.md` (feature-sliced layered layout, one-way dependency direction, `@/*` alias, starter-widget retirement plan) + `src/{design-system,features,shared,db}/` skeleton READMEs. 0.1 → Implemented; counts 1 Implemented / 10 Specified / 56 Blocked |
| ea77269 | 2026-07-13 | UI Kit Coverage (sections 3–10 screens) | Surface the 2.6 quality-readiness gate in the UI Kit Coverage table: add "Quality readiness gate: 2.6" to the Blocked reason of every production screen (24 rows), keeping theme and app-bar (bootstrap shell) unchanged. Docs-only, coverage table only — no dependency-graph or status-count change |
| 2d00d10 | 2026-07-13 | 1.4, 1.5, 1.8, 2.3, 2.4, 2.6, 8.2, 11.2, 3.3–10.3 | Enforce quality-harness ordering: move 1.4/1.5 automated tests into 1.8 (implementation contracts only); add 2.6 feature quality-harness readiness gate (deps 11.1/11.3/11.4); gate all production screen slices (3.3–10.3) on 2.6 without a cycle; add 0.13 + shared quality harnesses + 2.6 to the Critical Path and MVP A; correct MVP A gates (DEP-DB, DEP-TEST, DEP-ICON-FONT, DEP-GOLDEN, DEP-TTS); fix stale 0.8/0.4 evidence (1.8/2.4/11.2 → 0.13; 2.3/8.2 persistence → 0.5); recount (67 rows: 11 Specified / 56 Blocked) |
| a4155ee | 2026-07-13 | 0.6, 0.7, 0.8, 0.13, 1.5, 1.6, 1.7, 2.1, 2.2, 2.3, 3.1, 3.2, 4.1, 4.2, 5.1, 5.2, 8.2, 11.1, 11.3, 11.4, 11.6 | Fix test and quality prerequisites: split the test-framework decision (0.8) from the harness setup (0.13, new); add the test-harness prerequisite to test-owning domain/persistence rows (0.6, 3.1, 3.2, 4.1, 4.2, 5.1, 5.2 → Blocked); gate icon-based primitives 1.5/1.6/1.7 on 1.4 (Blocked, cascading 0.7/2.1/2.2); convert 11.1/11.3/11.4 into harness/contract rows + add Vertical Slice Quality Ownership; expand 11.6 content-flow prerequisites; gate theme/reminder persistence on 0.5; recalculate work-package and status counts (66 rows: 11 Specified / 55 Blocked) |
| 093ea6e | 2026-07-13 | 0.4, 1.4, 10.2, 10.3, 11.6 | Fix remaining dependency gates: remove the DEP-DB self-cycle from the 0.4 decision gate; add DEP-ICON-FONT and set 1.4 Blocked; gate 10.2 and 10.3 behind MVP B (5.5 + 7.4); add DEP-FILE-PICKER/DEP-FILE-SHARING to 10.3; gate 11.6 smoke on import/export (9.1, 9.2); recalculate status counts (21 Specified / 44 Blocked) |
| d1f9bf4 | 2026-07-15 | 11.5 | Performance: library query budget fixed at 4 reads (batch countByDecks/dueCountByDeck) + AppScreen FlatList body virtualizing flashcard-list + 1.94 MB cold-start snapshot, parity unchanged |
| 3e5aa53 | 2026-07-15 | 12.1, 12.2, 12.3, 12.4 | Fix navigation defects: create-deck creates via createDeck (+add-pair routing), study-hub children at settings/study/[section] (screen is a reserved RN param), stage-2 match tiles wired, card-editor deck context threaded + sheets close before navigating; nav suite 46/46 positive |
| 11b77ae | 2026-07-15 | 12.1–12.11 | Add section 12 (navigation and interaction test suite) from the 2026-07-15 audit: 4 unblock fixes + seeded nav-test harness + 5 per-area suites (back buttons + dialog open/close in scope) + dead-controls decision row; 78 rows = 62 Implemented / 10 Specified / 6 Blocked |
| 1458db1 | 2026-07-15 | 11.9 | Docs-code parity audit: 45 historical row hashes backfilled + 6.2 log TBD resolved + counts recomputed; governance row Implemented (61 Implemented / 6 Blocked) |
| d4fc849 | 2026-07-15 | 10.1 | Settings hub slice: 7-state hierarchical settings + persisted StudySettings feeding session shuffle + ProfileCard/SelectSheet composites, gated < 3% |
| c1cc147 | 2026-07-15 | 9.2 | Export slice: 4-state deck export + tested import round-trip + expo-file-system/expo-sharing delivery (DEP-FILE-SHARING), gated < 3% |
| 8c3ebd5 | 2026-07-15 | 9.1 | Import slice: 7-state file/paste import + compensated bulk insert with rollback + expo-document-picker (DEP-FILE-PICKER), gated < 3% |
| c41c9ac | 2026-07-14 | 8.2 | Reminder slice: 3-state reminder settings + app_setting persistence + expo-notifications weekly triggers (DEP-NOTIFICATIONS), gated < 3% |
| 1f5d837 | 2026-07-14 | 8.1 | Statistics slice: 5-state charts tab + DB-derived streaks/heatmap/Leitner/accuracy + Ring over react-native-svg (DEP-SVG), gated < 3% |
| 300337c | 2026-07-14 | 7.3 | Player slice: 5-state hands-free TTS playback + transport/speed + pause glyph re-subset, gated < 3% |
| 6a25d19 | 2026-07-14 | 5.5 | Study-session shell: 10-state 5-stage orchestrator + resume/save-error recovery + relearn/due-review, gated < 3% |
| 69fa2e1 | 2026-07-14 | 6.1 | Review-mode slice: 6-state browse round + one-attempt-per-card persistence + inline edit + TTS, gated < 3% |
| 977ea90 | 2026-07-14 | 4.4 | Flashcard-editor slice: 9-state card form + expo-speech (DEP-TTS approved) + Spinner/footer slot + icon-font re-subset, gated < 3% |
| 5b48622 | 2026-07-14 | 7.4 | Study-result slice: 7-state summary + transactional finalize/retry + shared Stat + variable web font-face, gated < 3% |
| 1b28f85 | 2026-07-14 | 5.3 | Dashboard slice: 8-state Today tab + ProgressBar/Note composites + live streak/goal/mastery derivation, gated < 3% |
| c0571c3 | 2026-07-14 | 4.3 | Flashcard-list slice: 15-state card list + shared StatusCardRow + overlay line-box/focus-ring parity fixes, gated < 3% |
| 6fcdfd3 | 2026-07-14 | 3.5 | Subdeck-list slice: 13-state screen + Breadcrumb/DeckPlaySheet composites + EmptyState/FAB parity fixes, gated < 3% |
| 917d017 | 2026-07-14 | 3.4 | Library slice: 12-state screen + container + shared DeckCard/Skeleton composites + goldens, parity-gated < 3% |
| 9f6b396 | 2026-07-14 | 0.9 | Dependency-register audit: better-sqlite3 retro-registered (DEP-DB-TESTDRIVER); standing WBS-loop pre-approval recorded; 0.9 → Implemented |
| e4ca0e3 | 2026-07-13 | 1.4, 1.5, 7.4, 8.1 | Fix WBS review findings: add MxList (18 primitives), correct 7.4↔8.1 dependency direction, reorder Next 10 (0.3 before 0.7), set 2.5 Blocked, normalize dependency ids to WBS/DEP-*, fix icon parity strategy |
| 7015e0b | 2026-07-13 | — | Create production implementation WBS from the UI Kit (docs-only) |

---

## Status Summary

78 work packages total (0.13 and 2.6 added; section 12 — navigation & interaction test suite — added from the 2026-07-15 audit).

| Status | Count |
|---|---:|
| Implemented | 66 |
| Partial | 0 |
| Specified | 6 |
| Blocked | 6 |
| Future | 0 |
| Deprecated | 0 |

`Implemented` = 0.1 (folder architecture), 0.2 (Expo Router route tree), 0.3 (state-management ADR), 0.10 (UI-Kit→RN mapping), 0.4 (local DB — DEP-DB approved), 0.8 (test-framework decision — DEP-TEST approved), 0.11 (`data-mx-node`→`testID` mapping rule), 1.1 (tokens → typed TS, `src/design-system/tokens/`), 1.2 (ThemeProvider + light/dark, mounted in root layout), 1.3 (Plus Jakarta Sans loaded via `expo-font`, splash-gated), 0.13 (production test harness — jest-expo + RNTL, `npm test` + CI wired), 0.6 (`Result<T,E>` + `AppError` model + user-facing mapping, unit-tested), 0.7 (repository/use-case + transaction/observable contracts in `src/shared/contracts/`, conformance-tested), 3.1 (Deck/subdeck/language domain — pure-TS entities + use cases + no-orphan/no-cycle invariants, unit-tested), 4.1 (Flashcard domain — Card + CardTranslation entities, duplicate detection, validation, unit-tested), 5.1 (Study/session + SRS domain — session state machine + 5-stage ordering + deterministic SM-2 scheduler + resume, unit-tested). The pure-domain layer (3.1/4.1/5.1) is complete, and the persistence framework (0.5) is in and the **persistence layer is complete** — content, flashcard, and session/SRS slices (3.2, 4.2, 5.2) all landed over real SQLite. `Specified` = 11.9 (governance — 0.9 register audit now Implemented; 0.12 raw-value guard now Implemented + machine-enforced in `npm run lint`) + every feature screen open past the 2.6 gate is now built — **7.4 Study-result is Implemented** (8 golden pairs incl. dark, parity-gated < 3%; finalize is the transactional `finalizeSessionUseCase` with an on-screen retry; all result numbers derived from the session + attempt history; the web build now registers the variable font with the kit's `font-weight: 200 800` descriptors). Only 11.9 (docs-code parity governance) remains Specified. **5.3 Dashboard is built** (9 golden pairs incl. dark, parity-gated < 3%; live numbers derived from decks + SRS + session history — due summary, daily-goal minutes, words learned, streak, library mastery; the tab shell gained the kit nav icons and a preview-theme override so dark goldens theme the real tab bar). **4.3 Flashcard-list is built** (16 golden pairs incl. dark, parity-gated < 3%; shared `StatusCardRow` composite; the overlay composites gained kit-accurate compact title line boxes and the modal focus-ring artifact fix). **All four non-TTS study modes are built** — 6.2 Match, 6.3 Guess, 7.1 Recall + 7.2 Fill run on the shared study-play stack (start → step → `recordAnswer` → finalize) with shared `study-chrome` + `session-fakes`; only 6.1 Review remains (DEP-TTS-gated). The remaining feature screens are the two big list/dashboard surfaces (4.3 Flashcard-list, 5.3 Dashboard) — **3.4 Library and 3.5 Subdeck-list are built (25 states, parity-gated < 3%)** — + 7.4 Study-result + governance (11.9). **3.3 Languages, 3.6 Deck-content-choice, 5.4 Mode-picker, 4.5 Deck-settings + 4.6 Search are built** (deck domain gained `reorganiseDeck`/`setDeckContentUseCase`/`moveDeck`/`moveDeckUseCase`; a transactional `resetDeckProgress` clears SRS state + attempts; 4.6 added a local search index + recent-history persistence and **cascade-unblocked the P0 Library screen 3.4**; overlays now use RN `Modal`). **The 2.6 gate is cleared and the first feature screen (3.3 Languages) is built** — feature screens now build in dependency waves, each cascade-unblocking the next. 3.3 is the first production screen: a prop-driven `LanguagesScreen` (loading/error/empty/one/list/add-form/remove-dialog) + a repository-wired controller (deck-count subtitles from 3.2) + committed golden baselines. **Design-system section 1 is fully complete (1.1–1.8).** `Blocked` = the remaining 42 rows, gated on the icon font (1.4/DEP-ICON-FONT), the feature quality-harness readiness gate (2.6, via 11.1/11.3/11.4), or another unapproved capability (DEP-GOLDEN, DEP-TTS, DEP-NOTIFICATIONS, DEP-FILE-PICKER, DEP-FILE-SHARING, DEP-AUTH, DEP-CLOUD). The 2.6 gate is Implemented; 12 feature screens are now Specified and the rest unblock as their feature deps land, the 2.6 gate and every feature slice 3.3–10.3 (gated on 2.6) are Blocked; 35 + 0 + 15 + 17 + 0 + 0 = 67. Section 1 (design system) is done; the app shell (2.1→2.2→2.4→2.5), theme screen (2.3), the a11y (11.3) + visual (11.4) harnesses and the 2.6 gate are the path to the feature screens. With the icon font approved and the primitive families unblocked, the UI build order is now open: 1.5/1.6/1.7 primitives → 1.8 tests → 2.x shell → 11.3/11.4 → 2.6 → feature screens. The critical path to any feature screen now runs through **DEP-ICON-FONT** (1.4 icon adapter → primitives → shell → 11.3/11.4 → 2.6). Production foundation is being laid decision-by-decision; feature screens remain scaffolds until 2.6.
