/**
 * Repository + use-case contracts (WBS 0.7). Engine-independent interfaces the
 * domain and data layers depend on; built on the WBS 0.6 `Result` kernel.
 */

export * from './use-case';
export * from './repository';
export * from './services';
