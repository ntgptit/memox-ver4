# shared

Cross-cutting kernel with no feature/UI imports: `result.ts` (Result<T,E> + errors, WBS 0.6),
`contracts/` (repository/use-case interfaces, WBS 0.7), `testing/` (shared fixtures/helpers,
WBS 11.1/11.3). See `docs/adr/0001-architecture.md`.
