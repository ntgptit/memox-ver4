/**
 * Search data layer (WBS 4.6) — the local library search query + recent-history
 * persistence. Read paths for the search screen (5.x UI).
 */

export * from './search-query';
export * from './recent-searches';
