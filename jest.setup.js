// Global test setup — WBS 0.13.
// @testing-library/react-native v12.4+ registers its jest matchers automatically, and
// the jest-expo preset mocks the Expo native modules, so no manual wiring is needed yet.
// This file is the seam for future global test setup (fake timers, native-module mocks,
// repository test fixtures) as the feature slices add them.
